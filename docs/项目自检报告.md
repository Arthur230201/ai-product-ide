# 项目自检报告

生成时间: 2026-03-10T04:59:28.203Z

## 摘要

- Build: pass
- Lint: pass
- TypeCheck: pass
- LLM 调用入口: 11 个文件
- JSON 解析相关: 28 个文件
- 网络错误处理: 10 个文件
- substring 使用: 23 个文件
- HTML 渲染相关: 11 个文件

## 详细结果

### Build 检查
```json
{
  "status": "pass",
  "duration": 29635
}
```

### Lint 检查
```json
{
  "status": "pass",
  "duration": 1644
}
```

### TypeCheck 检查
```json
{
  "status": "pass",
  "duration": 2448
}
```

### LLM 调用入口清单
共找到 11 个文件包含 LLM 调用：

- **app/actions/generate-graph.ts**: 2 处调用
- **app/actions/generate-tailwind-config.ts**: 1 处调用
- **app/actions/node-operations.ts**: 3 处调用
- **app/actions/parse-topology.ts**: 1 处调用
- **app/api/extract-theme/route.ts**: 2 处调用
- **app/api/generate-prd/route.ts**: 1 处调用
- **app/api/page-plan/synthesize/route.ts**: 2 处调用
- **lib/ai/llm.ts**: 4 处调用
- **lib/ai-config.ts**: 4 处调用
- **lib/openai-client.ts**: 2 处调用
- **lib/ui/ui-compiler.ts**: 5 处调用

### JSON 解析失败链路
共找到 28 个文件包含 JSON 解析相关代码：

- **app/actions/generate-graph.ts**: 1 处
- **app/actions/node-operations.ts**: 3 处
- **app/actions/ui-pipeline-response.test.ts**: 3 处
- **app/api/page-plan/synthesize/route.ts**: 3 处
- **components/canvas/CommandBar.tsx**: 9 处
- **components/canvas/HtmlSandboxRenderer.tsx**: 1 处
- **components/canvas/NodeDetailPanel.tsx**: 4 处
- **components/canvas/ProjectRecovery.tsx**: 2 处
- **components/canvas/ProjectToolbar.tsx**: 4 处
- **components/canvas/StyleExtractor.tsx**: 1 处
- **lib/ai/json-extract.test.ts**: 5 处
- **lib/ai/json-extract.ts**: 1 处
- **lib/ai/llm-mock.ts**: 5 处
- **lib/ai/llm.ts**: 3 处
- **lib/page-plan/__tests__/buildDataQueryRuntime.test.ts**: 5 处
- **lib/page-plan/__tests__/validatePlan.test.ts**: 4 处
- **lib/page-plan/getRegistrySnippet.ts**: 1 处
- **lib/page-plan/parsePagePlanFromText.ts**: 2 处
- **lib/page-plan/render/buildDataQueryRuntime.ts**: 1 处
- **lib/safe/preview.ts**: 1 处
- **lib/ui/injector.ts**: 1 处
- **lib/ui/prompt-budget.ts**: 1 处
- **lib/ui/ui-compiler.ts**: 3 处
- **store/canvas-store.ts**: 4 处
- **utils/html-config-extractor.ts**: 1 处
- **utils/html-to-jsx-transmuter.ts**: 1 处
- **utils/html-to-react-adapter.ts**: 1 处
- **utils/prdGenerator.ts**: 1 处

### 429/ECONNRESET 错误处理
共找到 10 个文件包含网络错误处理：

- **app/actions/generate-graph.ts**: 6 处
- **app/actions/node-operations.ts**: 7 处
- **app/actions/ui-pipeline-response.test.ts**: 6 处
- **app/actions/ui-pipeline-response.ts**: 8 处
- **components/canvas/CommandBar.tsx**: 9 处
- **lib/ai/llm.ts**: 41 处
- **lib/graph/fallback-graph.ts**: 1 处
- **lib/openai-client.ts**: 17 处
- **lib/ui-generation-helpers.ts**: 33 处
- **lib/user-facing-messages.ts**: 1 处

### uiCode.substring 使用
共找到 23 个文件包含 substring 调用：

- **app/actions/generate-graph.ts**: 6 处
- **app/actions/node-operations.ts**: 12 处
- **app/actions/parse-topology.ts**: 1 处
- **components/canvas/CommandBar.tsx**: 23 处
- **components/canvas/HtmlFirstPreview.tsx**: 2 处
- **components/canvas/MermaidDiagram.tsx**: 1 处
- **components/canvas/ProjectBlueprint.tsx**: 3 处
- **components/canvas/WordUploader.tsx**: 1 处
- **lib/ai/gateway-queue.ts**: 3 处
- **lib/ai/json-extract.ts**: 5 处
- **lib/ai/llm.ts**: 2 处
- **lib/ai/protocol.ts**: 1 处
- **lib/openai-client.ts**: 1 处
- **lib/preview-ui-prd-bundle.generated.ts**: 1 处
- **lib/safe/preview.ts**: 2 处
- **lib/ui/prompt-budget.ts**: 3 处
- **lib/ui/ui-compiler.ts**: 2 处
- **lib/ui-generation-helpers.ts**: 2 处
- **utils/codeToPrdTable.ts**: 3 处
- **utils/demo-router.ts**: 1 处
- **utils/html-parser.ts**: 3 处
- **utils/prdGenerator.ts**: 4 处
- **utils/ui-summary-extractor.ts**: 4 处

### HTML 渲染危险点
共找到 11 个文件包含 HTML 渲染相关代码：

- **app/actions/generate-tailwind-config.ts**: 1 处
- **components/canvas/BabelExecutor.tsx**: 4 处
- **components/canvas/HtmlSandbox.tsx**: 1 处
- **components/canvas/HtmlSandboxRenderer.tsx**: 3 处
- **components/canvas/LivePreview.tsx**: 2 处
- **components/canvas/MermaidDiagram.tsx**: 1 处
- **components/canvas/PreviewFrame.tsx**: 1 处
- **components/canvas/ReactJitRenderer.tsx**: 3 处
- **utils/demo-router.ts**: 2 处
- **utils/html-to-react-adapter.ts**: 1 处
- **utils/prdGenerator.ts**: 7 处

## 完整 JSON 数据

```json
{
  "timestamp": "2026-03-10T04:59:28.203Z",
  "checks": {
    "build": {
      "status": "pass",
      "duration": 29635
    },
    "lint": {
      "status": "pass",
      "duration": 1644
    },
    "typecheck": {
      "status": "pass",
      "duration": 2448
    },
    "llmEntries": {
      "count": 11,
      "entries": [
        {
          "file": "app/actions/generate-graph.ts",
          "matches": [
            {
              "type": "getVisionModel",
              "line": 238,
              "context": "== 'image';\n  const model = useVisionModel \n    ? getVisionModel(aiConfig)\n    : textModel;\n  \n  log('🔍 [analyzeInputClarity] 开始分析输入模糊度', {\n    reque"
            },
            {
              "type": "getTextModel",
              "line": 508,
              "context": "   const textModel = input.aiConfig?.textModel || getTextModel();\n\n    // 意图加工：用户输入 → 增强后的用户 prompt（含领域范围说明）\n    const rawPrompt = input.prompt.trim()"
            }
          ]
        },
        {
          "file": "app/actions/generate-tailwind-config.ts",
          "matches": [
            {
              "type": "getTextModel",
              "line": 124,
              "context": "他说明文字。`;\n\n      // 调用 AI 生成配置\n      const model = getTextModel();\n      const result = await callText({\n        model,\n        prompt: `${systemPrompt"
            }
          ]
        },
        {
          "file": "app/actions/node-operations.ts",
          "matches": [
            {
              "type": "getTextModel",
              "line": 273,
              "context": "常见做法进行合理推断`;\n\n    // 获取模型配置\n    const textModel = getTextModel(input.aiConfig);\n    \n    // ✅ 使用 callText 统一 gateway\n    const result = await callText"
            },
            {
              "type": "getTextModel",
              "line": 395,
              "context": "`;\n\n    // 获取模型配置（使用默认文本模型）\n    const textModel = getTextModel();\n    \n    // ✅ 使用 callText 统一 gateway\n    const result = await callText({\n      model"
            },
            {
              "type": "getTextModel",
              "line": 1502,
              "context": " : ''}`;\n\n      // 获取模型配置\n      const textModel = getTextModel(input.aiConfig);\n      \n      // ✅ 使用 callText 统一 gateway\n      const result = await ca"
            }
          ]
        },
        {
          "file": "app/actions/parse-topology.ts",
          "matches": [
            {
              "type": "getVisionModel",
              "line": 90,
              "context": "     }\n\n      // 获取模型配置\n      const visionModel = getVisionModel(input.aiConfig);\n      log(`📸 [parseTopology] 图片数据验证通过，开始调用 ${visionModel} Vision`);"
            }
          ]
        },
        {
          "file": "app/api/extract-theme/route.ts",
          "matches": [
            {
              "type": "generateObject",
              "line": 74,
              "context": "erateObject 强制返回结构化 JSON\n    const result = await generateObject({\n      model: openai(visionModel),\n      schema: UIThemeConfigSchema,\n      messages"
            },
            {
              "type": "getVisionModel",
              "line": 70,
              "context": "[1];\n    }\n\n    // 获取模型配置\n    const visionModel = getVisionModel(aiConfig);\n    log(`📸 [extract-theme] 图片数据准备完成，开始调用 ${visionModel} Vision`);\n\n    //"
            }
          ]
        },
        {
          "file": "app/api/generate-prd/route.ts",
          "matches": [
            {
              "type": "generateText",
              "line": 128,
              "context": "的需求\n5. 输出 Markdown 格式`;\n\n    const result = await generateText({\n      model: openai('gpt-4o'),\n      messages: [\n        {\n          role: 'system',\n"
            }
          ]
        },
        {
          "file": "app/api/page-plan/synthesize/route.ts",
          "matches": [
            {
              "type": "generateText",
              "line": 94,
              "context": "RegistrySnippet,\n    });\n    const result = await generateText({\n      model: openai('gpt-4o'),\n      prompt,\n      temperature: 0.2,\n      maxOutputT"
            },
            {
              "type": "generateText",
              "line": 110,
              "context": "RegistrySnippet,\n    });\n    const result = await generateText({\n      model: openai('gpt-4o'),\n      prompt,\n      temperature: 0.1,\n      maxOutputT"
            }
          ]
        },
        {
          "file": "lib/ai/llm.ts",
          "matches": [
            {
              "type": "generateText",
              "line": 266,
              "context": "OutputTokens;\n      }\n\n      const result = await generateText(generateOptions);\n\n      clearTimeout(timeoutId);\n      const elapsed = Date.now() - st"
            },
            {
              "type": "generateObject",
              "line": 542,
              "context": "e provided');\n      }\n\n      const result = await generateObject(generateOptions);\n\n      clearTimeout(timeoutId);\n      const elapsed = Date.now() - "
            },
            {
              "type": "getOpenAIClient",
              "line": 222,
              "context": "Initialize OpenAI client\n    const openaiClient = getOpenAIClient(aiConfig);\n\n    // Create AbortController for timeout\n    const controller = new Abo"
            },
            {
              "type": "getOpenAIClient",
              "line": 508,
              "context": "Initialize OpenAI client\n    const openaiClient = getOpenAIClient(aiConfig);\n\n    // Create AbortController for timeout\n    const controller = new Abo"
            }
          ]
        },
        {
          "file": "lib/ai-config.ts",
          "matches": [
            {
              "type": "getVisionModel",
              "line": 64,
              "context": "sion : fastText;\n  }\n  return type === 'vision' ? getVisionModel(userConfig) : getTextModel(userConfig);\n}\n\n/**\n * 获取视觉模型（用于 UI 生成、拓扑解析等）\n * 优先级：用户配置 "
            },
            {
              "type": "getVisionModel",
              "line": 71,
              "context": "解析等）\n * 优先级：用户配置 > 环境变量 > 默认值\n */\nexport function getVisionModel(userConfig?: { visionModel?: string }): string {\n  if (userConfig?.visionModel) retur"
            },
            {
              "type": "getTextModel",
              "line": 64,
              "context": " type === 'vision' ? getVisionModel(userConfig) : getTextModel(userConfig);\n}\n\n/**\n * 获取视觉模型（用于 UI 生成、拓扑解析等）\n * 优先级：用户配置 > 环境变量 > 默认值\n */\nexport funct"
            },
            {
              "type": "getTextModel",
              "line": 83,
              "context": "分析等）\n * 优先级：用户配置 > 环境变量 > 默认值\n */\nexport function getTextModel(userConfig?: { textModel?: string }): string {\n  if (userConfig?.textModel) return user"
            }
          ]
        },
        {
          "file": "lib/openai-client.ts",
          "matches": [
            {
              "type": "getOpenAIClient",
              "line": 286,
              "context": "nstance (createOpenAI result)\n */\nexport function getOpenAIClient(aiConfig?: { textModel?: string; visionModel?: string }) {\n  const apiKey = ensureOp"
            },
            {
              "type": "getOpenAIClient",
              "line": 310,
              "context": "tring; visionModel?: string }) {\n  const client = getOpenAIClient(aiConfig);\n  // Return a function that creates model instances (same signature as op"
            }
          ]
        },
        {
          "file": "lib/ui/ui-compiler.ts",
          "matches": [
            {
              "type": "generateText",
              "line": 90,
              "context": "SON must be valid`;\n\n    const specResult = await generateText({\n      model: openaiClient(textModel),\n      messages: [\n        { role: 'system', con"
            },
            {
              "type": "generateText",
              "line": 160,
              "context": "from user request`;\n\n    const htmlResult = await generateText({\n      model: openaiClient(textModel),\n      messages: [\n        { role: 'system', con"
            },
            {
              "type": "generateText",
              "line": 226,
              "context": "p output concise`;\n\n    const patchResult = await generateText({\n      model: openaiClient(textModel),\n      messages: [\n        { role: 'system', con"
            },
            {
              "type": "getOpenAIClient",
              "line": 53,
              "context": " getTextModel(aiConfig);\n    const openaiClient = getOpenAIClient(aiConfig);\n\n    // ==================== Track 1: Build UISpec (AI JSON, small budget"
            },
            {
              "type": "getTextModel",
              "line": 52,
              "context": "6).substr(2, 9)}`;\n\n  try {\n    const textModel = getTextModel(aiConfig);\n    const openaiClient = getOpenAIClient(aiConfig);\n\n    // ================"
            }
          ]
        }
      ]
    },
    "jsonParseIssues": {
      "count": 28,
      "entries": [
        {
          "file": "app/actions/generate-graph.ts",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 620,
              "hasErrorHandling": true,
              "context": "needed / graph_generated / SingleCallResult\n        let raw: unknown;\n        try {\n          raw = JSON.parse(responseText.trim());\n        } catch {\n          raw = null;\n        }\n        const type = raw && typeof raw === 'object' && 'type' in raw ? (raw as { type?: string }).type : undefined;\n\n"
            }
          ]
        },
        {
          "file": "app/actions/node-operations.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 981,
              "hasErrorHandling": true,
              "context": "') {\n        errorMessage = (error as any).message || (error as any).error || (error as any).msg || JSON.stringify(error);\n        logError('❌ [generateUIFromImage] 错误类型: Object, 内容:', error);\n      } else {\n        errorMessage = String(error);\n        logError('❌ [generateUIFromImage] 错误类型: Other,"
            },
            {
              "type": "JSON.stringify",
              "line": 1069,
              "hasErrorHandling": true,
              "context": "ugPath = join(process.cwd(), 'scripts', 'viewport-debug-last.json');\n      writeFileSync(debugPath, JSON.stringify({\n        viewportUsed: viewportPreset,\n        received: input.viewportPreset,\n        requestId,\n        timestamp: new Date().toISOString(),\n      }, null, 2), 'utf8');\n    } catch ("
            },
            {
              "type": "JSON.stringify",
              "line": 1365,
              "hasErrorHandling": true,
              "context": "') {\n        errorMessage = (error as any).message || (error as any).error || (error as any).msg || JSON.stringify(error);\n      } else {\n        errorMessage = String(error);\n      }\n      \n      if (errorMessage.includes('API key') || errorMessage.includes('OPENAI')) {\n        throw new Error(`UI代"
            }
          ]
        },
        {
          "file": "app/actions/ui-pipeline-response.test.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 22,
              "hasErrorHandling": true,
              "context": "ror;\n  }\n}\n\nfunction assertEqual<T>(actual: T, expected: T, message?: string) {\n  const actualStr = JSON.stringify(actual);\n  const expectedStr = JSON.stringify(expected);\n  if (actualStr !== expectedStr) {\n    throw new Error(\n      `${message || 'Assertion failed'}: expected ${expectedStr}, got ${"
            },
            {
              "type": "JSON.stringify",
              "line": 23,
              "hasErrorHandling": true,
              "context": " expected: T, message?: string) {\n  const actualStr = JSON.stringify(actual);\n  const expectedStr = JSON.stringify(expected);\n  if (actualStr !== expectedStr) {\n    throw new Error(\n      `${message || 'Assertion failed'}: expected ${expectedStr}, got ${actualStr}`\n    );\n  }\n}\n\nfunction assertType<"
            },
            {
              "type": "JSON.stringify",
              "line": 33,
              "hasErrorHandling": true,
              "context": " v is T): asserts value is T {\n  if (!guard(value)) {\n    throw new Error(`Type assertion failed: ${JSON.stringify(value)}`);\n  }\n}\n\n// Test cases\ntest('normalizeUIPipelineResponse: already valid UIPipelineResponse (success)', () => {\n  const valid: UIPipelineResponse = {\n    ok: true,\n    type: 'UI"
            }
          ]
        },
        {
          "file": "app/api/page-plan/synthesize/route.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 71,
              "hasErrorHandling": true,
              "context": "us: 400 }\n    );\n  }\n\n  const templateRules = templateRulesMap[templateId];\n  const graphNodeJson = JSON.stringify(\n    {\n      id: nodeId,\n      label: nodeLabel || nodeId,\n      pageType,\n      dataQueries: dataQueries.map((q) => ({ id: q.id, description: q.description })),\n      events: events.ma"
            },
            {
              "type": "JSON.stringify",
              "line": 123,
              "hasErrorHandling": true,
              "context": "tempt++) {\n    const raw = attempt === 0 ? await callSynthesize() : await callRepair(lastPlanJson!, JSON.stringify(lastErrors, null, 2));\n    const plan = parsePagePlanFromText(raw);\n    if (!plan) {\n      return NextResponse.json(\n        {\n          ok: false,\n          error: 'LLM 输出无法解析为 PagePla"
            },
            {
              "type": "JSON.stringify",
              "line": 135,
              "hasErrorHandling": true,
              "context": "       raw: raw.slice(0, 500),\n        },\n        { status: 200 }\n      );\n    }\n    lastPlanJson = JSON.stringify(plan, null, 2);\n    const validated = validatePlan(plan, { templateRules });\n    if (validated.ok) {\n      return NextResponse.json({ ok: true, plan: validated.plan });\n    }\n    lastEr"
            }
          ]
        },
        {
          "file": "components/canvas/CommandBar.tsx",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 149,
              "hasErrorHandling": false,
              "context": "object' ? Object.keys(result as any) : [],\n        // 尝试序列化查看完整结构（限制长度避免日志过大）\n        resultString: JSON.stringify(result).substring(0, 500),\n        // 直接检查 result.data 的内容\n        dataExists: !!(result as any)?.data,\n        dataType: typeof (result as any)?.data,\n        dataKeys: (result as any)"
            },
            {
              "type": "JSON.stringify",
              "line": 164,
              "hasErrorHandling": false,
              "context": "data.edges.length : 'N/A',\n        // 完整的数据结构（限制长度）\n        fullDataString: (result as any)?.data ? JSON.stringify((result as any).data).substring(0, 1000) : 'N/A',\n      });\n\n      // 处理 zsa-react 的返回格式\n      // zsa-react 的 useServerAction 返回格式：result 直接是 handler 的返回值\n      // 所以 result 应该是 { data:"
            },
            {
              "type": "JSON.stringify",
              "line": 236,
              "hasErrorHandling": false,
              "context": "tData as any).data.nodes.length : 'N/A',\n        dataNodesValue: (resultData as any)?.data?.nodes ? JSON.stringify((resultData as any).data.nodes).substring(0, 200) : 'N/A',\n        dataEdgesType: typeof (resultData as any)?.data?.edges,\n        dataEdgesIsArray: Array.isArray((resultData as any)?.d"
            },
            {
              "type": "JSON.stringify",
              "line": 248,
              "hasErrorHandling": false,
              "context": "sIsArray: Array.isArray((resultData as any)?.edges),\n        // 完整结构预览（限制长度）\n        fullStructure: JSON.stringify(resultData).substring(0, 1000),\n      });\n\n      // 提取 nodes 和 edges\n      // 尝试多种可能的路径\n      let nodes: any[] | undefined;\n      let edges: any[] | undefined;\n\n      // 优先尝试 resultData"
            },
            {
              "type": "JSON.stringify",
              "line": 297,
              "hasErrorHandling": false,
              "context": "edgesIsArray: Array.isArray(data.edges),\n          // 完整 data 对象的结构（限制长度）\n          fullDataString: JSON.stringify(data).substring(0, 1000),\n        });\n        \n        // 如果 data 中只有一个键，且这个键的值是对象，尝试从中提取 nodes/edges（不再处理 clarification_needed，避免无法继续）\n        if (dataKeys.length === 1 && typeof data["
            },
            {
              "type": "JSON.stringify",
              "line": 325,
              "hasErrorHandling": false,
              "context": "|| 0 });\n        } else if (!nodes) {\n          const nodesStringified = data.nodes !== undefined ? JSON.stringify(data.nodes).substring(0, 200) : 'undefined';\n          logWarn('⚠️ [CommandBar] resultData.data.nodes 不是数组:', {\n            type: typeof data.nodes,\n            value: data.nodes,\n     "
            },
            {
              "type": "JSON.stringify",
              "line": 339,
              "hasErrorHandling": false,
              "context": "|| 0 });\n        } else if (!edges) {\n          const edgesStringified = data.edges !== undefined ? JSON.stringify(data.edges).substring(0, 200) : 'undefined';\n          logWarn('⚠️ [CommandBar] resultData.data.edges 不是数组:', {\n            type: typeof data.edges,\n            value: data.edges,\n     "
            },
            {
              "type": "JSON.stringify",
              "line": 754,
              "hasErrorHandling": true,
              "context": "❌ [CommandBar] Failed to update node:', errorInfo);\n      logError('❌ [CommandBar] Error details:', JSON.stringify(errorInfo, null, 2));\n      logError('❌ [CommandBar] Raw error:', error);\n      \n      // Error handled\n      \n      let errorMessage = error instanceof Error \n        ? error.message \n"
            },
            {
              "type": "JSON.stringify",
              "line": 1825,
              "hasErrorHandling": true,
              "context": "             errorMessage = (error as any).message || (error as any).error || (error as any).msg || JSON.stringify(error);\n              errorStack = (error as any).stack;\n            } else {\n              errorMessage = String(error);\n            }\n          } catch (e) {\n            errorMessage "
            }
          ]
        },
        {
          "file": "components/canvas/HtmlSandboxRenderer.tsx",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 448,
              "hasErrorHandling": true,
              "context": "d: { byRef?: Record<string, { columns?: string[]; rows?: string[][] }> };\n    try {\n      payload = JSON.parse(script.textContent);\n    } catch {\n      return;\n    }\n    const byRef = payload.byRef || {};\n    const cellClass = 'border-b border-slate-100 px-4 py-3';\n    let hasAnyRows = false;\n    do"
            }
          ]
        },
        {
          "file": "components/canvas/NodeDetailPanel.tsx",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 79,
              "hasErrorHandling": false,
              "context": "equirements;\n  const requirementsKey = useMemo(() => {\n    if (!requirements) return '';\n    return JSON.stringify(requirements);\n  }, [requirements]);\n  \n  const storeSpecText = useMemo(() => {\n    if (!requirements) return '';\n    return Array.isArray(requirements) \n      ? requirements.join('\\n')"
            },
            {
              "type": "JSON.stringify",
              "line": 480,
              "hasErrorHandling": true,
              "context": "[selectedNode.data.artifacts.spec.requirements] : []);\n    \n    // 比较数组内容是否相同\n    const isChanged = JSON.stringify(reqs) !== JSON.stringify(currentReqs);\n    if (isChanged) {\n      updateNodeData(selectedNodeId, {\n        artifacts: { ...selectedNode.data.artifacts, spec: { ...selectedNode.data.arti"
            },
            {
              "type": "JSON.stringify",
              "line": 480,
              "hasErrorHandling": true,
              "context": "cts.spec.requirements] : []);\n    \n    // 比较数组内容是否相同\n    const isChanged = JSON.stringify(reqs) !== JSON.stringify(currentReqs);\n    if (isChanged) {\n      updateNodeData(selectedNodeId, {\n        artifacts: { ...selectedNode.data.artifacts, spec: { ...selectedNode.data.artifacts.spec, requirements:"
            },
            {
              "type": "JSON.stringify",
              "line": 819,
              "hasErrorHandling": true,
              "context": "-zinc-900 rounded-lg p-4 overflow-x-auto whitespace-pre-wrap font-mono\">\n                          {JSON.stringify(artifacts.impl.apiEndpoints, null, 2)}\n                        </pre>\n                      </section>\n                    )}\n                    {artifacts.impl?.dbSchema && artifacts."
            }
          ]
        },
        {
          "file": "components/canvas/ProjectRecovery.tsx",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 23,
              "hasErrorHandling": true,
              "context": "-canvas-storage');\r\n      setLocalStorageData(stored);\r\n      if (stored) {\r\n        const parsed = JSON.parse(stored);\r\n        setRecoveredData(parsed);\r\n      }\r\n    } catch (error) {\r\n      console.error('Error reading localStorage:', error);\r\n    }\r\n  }, []);\r\n\r\n  const handleRecoverFromLocalSt"
            },
            {
              "type": "JSON.parse",
              "line": 84,
              "hasErrorHandling": true,
              "context": "t) => {\r\n      try {\r\n        const content = event.target?.result as string;\r\n        const data = JSON.parse(content);\r\n        \r\n        // 支持两种格式：直接的项目数据或 localStorage 格式\r\n        let nodes, edges;\r\n        if (data.nodes && data.edges) {\r\n          // 直接的项目格式\r\n          nodes = data.nodes;\r\n   "
            }
          ]
        },
        {
          "file": "components/canvas/ProjectToolbar.tsx",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 103,
              "hasErrorHandling": true,
              "context": "> {\n      try {\n        const content = event.target?.result as string;\n        const projectData = JSON.parse(content);\n        \n        // 验证数据结构\n        if (!projectData || typeof projectData !== 'object') {\n          throw new Error('无效的项目文件格式');\n        }\n        \n        if (!Array.isArray(pro"
            },
            {
              "type": "JSON.stringify",
              "line": 64,
              "hasErrorHandling": true,
              "context": "ice(0, -5);\n      const filename = `project-${timestamp}.json`;\n      \n      const blob = new Blob([JSON.stringify(projectData, null, 2)], { type: 'application/json' });\n      const url = URL.createObjectURL(blob);\n      const link = document.createElement('a');\n      link.href = url;\n      link.dow"
            },
            {
              "type": "JSON.stringify",
              "line": 249,
              "hasErrorHandling": true,
              "context": "', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({\n          nodeId: node.id,\n          nodeLabel: node.data?.label || node.id,\n          pageType,\n          templateId,\n          dataQueries: dataQueries?.map((q) => ({ id: q.id, desc"
            },
            {
              "type": "JSON.stringify",
              "line": 299,
              "hasErrorHandling": true,
              "context": "te-prd', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({\n        nodes: nodes.map(node => ({\n          id: node.id,\n          label: node.data.label,\n          type: node.type,\n          spec: node.data.artifacts?.spec,\n        })),\n       "
            }
          ]
        },
        {
          "file": "components/canvas/StyleExtractor.tsx",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 56,
              "hasErrorHandling": true,
              "context": ": 'POST',\n        headers: {\n          'Content-Type': 'application/json',\n        },\n        body: JSON.stringify({ \n          imageBase64: base64,\n          aiConfig: aiConfig, // 传递 AI 模型配置\n        }),\n      });\n\n      if (!response.ok) {\n        const errorData = await response.json();\n        t"
            }
          ]
        },
        {
          "file": "lib/ai/json-extract.test.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 20,
              "hasErrorHandling": true,
              "context": ";\n    throw error;\n  }\n}\n\nfunction assertEqual<T>(actual: T, expected: T, message?: string) {\n  if (JSON.stringify(actual) !== JSON.stringify(expected)) {\n    throw new Error(`${message || 'Assertion failed'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`);\n  }\n}\n\nfunction ass"
            },
            {
              "type": "JSON.stringify",
              "line": 20,
              "hasErrorHandling": true,
              "context": "unction assertEqual<T>(actual: T, expected: T, message?: string) {\n  if (JSON.stringify(actual) !== JSON.stringify(expected)) {\n    throw new Error(`${message || 'Assertion failed'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`);\n  }\n}\n\nfunction assertNull(actual: unknown, me"
            },
            {
              "type": "JSON.stringify",
              "line": 21,
              "hasErrorHandling": true,
              "context": ") !== JSON.stringify(expected)) {\n    throw new Error(`${message || 'Assertion failed'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`);\n  }\n}\n\nfunction assertNull(actual: unknown, message?: string) {\n  if (actual !== null) {\n    throw new Error(`${message || 'Assertion failed"
            },
            {
              "type": "JSON.stringify",
              "line": 21,
              "hasErrorHandling": true,
              "context": "\n    throw new Error(`${message || 'Assertion failed'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`);\n  }\n}\n\nfunction assertNull(actual: unknown, message?: string) {\n  if (actual !== null) {\n    throw new Error(`${message || 'Assertion failed'}: expected null, got ${JSON.str"
            },
            {
              "type": "JSON.stringify",
              "line": 27,
              "hasErrorHandling": true,
              "context": " if (actual !== null) {\n    throw new Error(`${message || 'Assertion failed'}: expected null, got ${JSON.stringify(actual)}`);\n  }\n}\n\nfunction assert(condition: boolean, message?: string) {\n  if (!condition) {\n    throw new Error(message || 'Assertion failed');\n  }\n}\n\n// 测试用例\nfunction runTests() {\n "
            }
          ]
        },
        {
          "file": "lib/ai/json-extract.ts",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 91,
              "hasErrorHandling": true,
              "context": "'...' : text,\n      };\n    }\n\n    // 步骤2: 解析 JSON\n    let parsed: unknown;\n    try {\n      parsed = JSON.parse(jsonStr);\n    } catch (parseError) {\n      return {\n        ok: false,\n        error: `JSON parse error: ${parseError instanceof Error ? parseError.message : String(parseError)}`,\n        r"
            }
          ]
        },
        {
          "file": "lib/ai/llm-mock.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 39,
              "hasErrorHandling": false,
              "context": "me === 'generateGraph' || params.actionName?.includes('generateGraph')) {\n    const mockGraphJSON = JSON.stringify({\n      clarity: {\n        confidence: 85,\n        isVague: false,\n        detectedDomain: '产品展示',\n        detectedBusinessObject: '产品',\n        detectedAction: '展示',\n      },\n      gra"
            },
            {
              "type": "JSON.stringify",
              "line": 107,
              "hasErrorHandling": false,
              "context": "return JSON\n  if (params.actionName?.includes('analyzeInputClarity')) {\n    const mockClarityJSON = JSON.stringify({\n      confidence: 85,\n      isVague: false,\n      detectedDomain: '产品展示',\n      detectedBusinessObject: '产品',\n      detectedAction: '展示',\n    }, null, 2);\n    \n    return {\n      ok: "
            },
            {
              "type": "JSON.stringify",
              "line": 224,
              "hasErrorHandling": false,
              "context": "s: [],\n    } as z.infer<T>;\n    \n    return {\n      ok: true,\n      data: mockGraphData,\n      raw: JSON.stringify(mockGraphData),\n      metrics: {\n        queuedMs: 0,\n        dedupHit: false,\n        totalMs: 20,\n      },\n    };\n  }\n  \n  // For InputClarityAnalysis, return high confidence\n  if (pa"
            },
            {
              "type": "JSON.stringify",
              "line": 246,
              "hasErrorHandling": false,
              "context": ": '展示',\n    } as z.infer<T>;\n    \n    return {\n      ok: true,\n      data: mockAnalysis,\n      raw: JSON.stringify(mockAnalysis),\n      metrics: {\n        queuedMs: 0,\n        dedupHit: false,\n        totalMs: 20,\n      },\n    };\n  }\n\n  // Default: Return empty object (will trigger fallback)\n  const"
            },
            {
              "type": "JSON.stringify",
              "line": 261,
              "hasErrorHandling": false,
              "context": "back)\n  const mockData = {} as z.infer<T>;\n  \n  return {\n    ok: true,\n    data: mockData,\n    raw: JSON.stringify(mockData),\n    metrics: {\n      queuedMs: 0,\n      dedupHit: false,\n      totalMs: 20,\n    },\n  };\n}\n\n/**\n * Generate mock static HTML (Stage 1)\n * Must satisfy:\n * - Single HTML file w"
            }
          ]
        },
        {
          "file": "lib/ai/llm.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 189,
              "hasErrorHandling": false,
              "context": "  const promptHash = prompt ? stableHash(prompt) : '';\n  const messagesHash = messages ? stableHash(JSON.stringify(messages)) : '';\n  const attachmentsHash = attachments ? stableHash(attachments) : '';\n  const keyInput = `${model}:${actionName}:${promptHash || messagesHash}:${attachmentsHash}:${mode"
            },
            {
              "type": "JSON.stringify",
              "line": 475,
              "hasErrorHandling": true,
              "context": "  const promptHash = prompt ? stableHash(prompt) : '';\n  const messagesHash = messages ? stableHash(JSON.stringify(messages)) : '';\n  const attachmentsHash = attachments ? stableHash(attachments) : '';\n  const keyInput = `${model}:${actionName}:${promptHash || messagesHash}:${attachmentsHash}:${mode"
            },
            {
              "type": "JSON.stringify",
              "line": 560,
              "hasErrorHandling": true,
              "context": "dupHit,\n      });\n\n      return {\n        ok: true as const,\n        data: objectData,\n        raw: JSON.stringify(objectData),\n      };\n    } catch (error) {\n      clearTimeout(timeoutId);\n      const elapsed = Date.now() - startTime;\n\n      // Handle abort (timeout)\n      if (error instanceof Erro"
            }
          ]
        },
        {
          "file": "lib/page-plan/__tests__/buildDataQueryRuntime.test.ts",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 71,
              "hasErrorHandling": true,
              "context": "const inner = script.replace(/.*<script[^>]*>/, '').replace(/<\\/script>.*/, '');\n    const parsed = JSON.parse(inner);\n    assert(parsed.byRef != null);\n  });\n\n  test('empty dataQueries yields empty byRef', () => {\n    const payload = buildDataQueryRuntimePayload({\n      nodeId: 'n1',\n      dataQuer"
            },
            {
              "type": "JSON.stringify",
              "line": 24,
              "hasErrorHandling": true,
              "context": "r(msg ?? 'assert failed');\n}\n\nfunction assertEqual<T>(actual: T, expected: T, msg?: string) {\n  if (JSON.stringify(actual) !== JSON.stringify(expected)) {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconsole.l"
            },
            {
              "type": "JSON.stringify",
              "line": 24,
              "hasErrorHandling": true,
              "context": "}\n\nfunction assertEqual<T>(actual: T, expected: T, msg?: string) {\n  if (JSON.stringify(actual) !== JSON.stringify(expected)) {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconsole.log('\\n📦 buildDataQueryRunt"
            },
            {
              "type": "JSON.stringify",
              "line": 26,
              "hasErrorHandling": true,
              "context": "al) !== JSON.stringify(expected)) {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconsole.log('\\n📦 buildDataQueryRuntime tests\\n');\ntry {\n  test('mock payload has byRef with nodeId::queryId key', () => {\n    c"
            },
            {
              "type": "JSON.stringify",
              "line": 26,
              "hasErrorHandling": true,
              "context": " {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconsole.log('\\n📦 buildDataQueryRuntime tests\\n');\ntry {\n  test('mock payload has byRef with nodeId::queryId key', () => {\n    const payload = buildDataQueryRunt"
            }
          ]
        },
        {
          "file": "lib/page-plan/__tests__/validatePlan.test.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 24,
              "hasErrorHandling": true,
              "context": "r(msg ?? 'assert failed');\n}\n\nfunction assertEqual<T>(actual: T, expected: T, msg?: string) {\n  if (JSON.stringify(actual) !== JSON.stringify(expected)) {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconst lis"
            },
            {
              "type": "JSON.stringify",
              "line": 24,
              "hasErrorHandling": true,
              "context": "}\n\nfunction assertEqual<T>(actual: T, expected: T, msg?: string) {\n  if (JSON.stringify(actual) !== JSON.stringify(expected)) {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconst listRules = listTemplateRules "
            },
            {
              "type": "JSON.stringify",
              "line": 26,
              "hasErrorHandling": true,
              "context": "al) !== JSON.stringify(expected)) {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconst listRules = listTemplateRules as TemplateRules;\n\nconsole.log('\\n📦 validatePlan tests\\n');\ntry {\n  test('valid list fallba"
            },
            {
              "type": "JSON.stringify",
              "line": 26,
              "hasErrorHandling": true,
              "context": " {\n    throw new Error(\n      `${msg ?? 'assertEqual'}: expected ${JSON.stringify(expected)}, got ${JSON.stringify(actual)}`\n    );\n  }\n}\n\nconst listRules = listTemplateRules as TemplateRules;\n\nconsole.log('\\n📦 validatePlan tests\\n');\ntry {\n  test('valid list fallback plan passes', () => {\n    cons"
            }
          ]
        },
        {
          "file": "lib/page-plan/getRegistrySnippet.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 29,
              "hasErrorHandling": false,
              "context": "mplateRulesJson(templateId: TemplateId): string {\n  const rules = templateMap[templateId];\n  return JSON.stringify(rules, null, 2);\n}\n\n/** Short registry snippet for LLM: component names + key props (Button, Table, Input, Card, etc.). */\nconst REGISTRY_SNIPPET = `组件名与 props 枚举（仅允许以下）：\n- Button: vari"
            }
          ]
        },
        {
          "file": "lib/page-plan/parsePagePlanFromText.ts",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 13,
              "hasErrorHandling": true,
              "context": "*([\\s\\S]*?)```/);\n  if (jsonBlock) {\n    text = jsonBlock[1].trim();\n  }\n  try {\n    const parsed = JSON.parse(text) as unknown;\n    if (parsed && typeof parsed === 'object' && 'version' in parsed && 'sections' in parsed) {\n      return parsed as PagePlan;\n    }\n  } catch {\n    // try trimming more\n"
            },
            {
              "type": "JSON.parse",
              "line": 21,
              "hasErrorHandling": true,
              "context": "ore\n    text = text.replace(/^[\\s\\S]*?(\\{[\\s\\S]*\\})[\\s\\S]*$/, '$1');\n    try {\n      const parsed = JSON.parse(text) as unknown;\n      if (parsed && typeof parsed === 'object' && 'version' in parsed && 'sections' in parsed) {\n        return parsed as PagePlan;\n      }\n    } catch {\n      // ignore\n "
            }
          ]
        },
        {
          "file": "lib/page-plan/render/buildDataQueryRuntime.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 71,
              "hasErrorHandling": false,
              "context": "yRef };\n}\n\nexport function runtimePayloadToScript(payload: RuntimePayload): string {\n  const json = JSON.stringify(payload).replace(/</g, '\\\\u003c');\n  return `<script type=\"application/json\" id=\"data-query-runtime\">${json}</script>`;\n}\n"
            }
          ]
        },
        {
          "file": "lib/safe/preview.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 29,
              "hasErrorHandling": true,
              "context": "(x);\n  }\n  \n  // 对象类型：尝试 JSON.stringify\n  if (typeof x === 'object') {\n    try {\n      const json = JSON.stringify(x);\n      if (json.length <= max) return json;\n      return json.substring(0, max) + '...';\n    } catch {\n      return '[Object]';\n    }\n  }\n  \n  return String(x);\n}\n\n/**\n * 确保值为字符串类型，否"
            }
          ]
        },
        {
          "file": "lib/ui/injector.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 26,
              "hasErrorHandling": false,
              "context": " 'use strict';\n  \n  // ==================== State Management ====================\n  const state = ${JSON.stringify(initialState)};\n  \n  // ==================== PostMessage Helper ====================\n  function postToHost(type, payload) {\n    if (window.parent && window.parent !== window) {\n      wi"
            }
          ]
        },
        {
          "file": "lib/ui/prompt-budget.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 67,
              "hasErrorHandling": true,
              "context": "of x;\n  throw new Error(\n    `[PromptBudget] ${name} must be string, got ${type}. ` +\n    `Value: ${JSON.stringify(x).substring(0, 100)}`\n  );\n}\n\n/**\n * 估算token数（粗略：1 token ≈ 4 字符）\n */\nexport function estimateTokens(text: string): number {\n  return Math.ceil((text || '').length / 4);\n}\n\n/**\n * Promp"
            }
          ]
        },
        {
          "file": "lib/ui/ui-compiler.ts",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 111,
              "hasErrorHandling": true,
              "context": "iled to extract UI specification',\n      };\n    }\n\n    let uiSpec: UISpec;\n    try {\n      uiSpec = JSON.parse(specBlock) as UISpec;\n    } catch (parseError) {\n      logError('[UI Compiler] UISpec JSON parse error', { requestId, error: parseError });\n      return {\n        ok: false,\n        type: '"
            },
            {
              "type": "JSON.parse",
              "line": 241,
              "hasErrorHandling": true,
              "context": "h[] = [];\n    let patchesApplied = 0;\n\n    if (patchBlock) {\n      try {\n        const patchArray = JSON.parse(patchBlock) as Array<{\n          op: 'SET_ATTR' | 'SET_TEXT' | 'TOGGLE_CLASS';\n          selector: string;\n          name?: string;\n          value?: string;\n          className?: string;\n "
            },
            {
              "type": "JSON.stringify",
              "line": 132,
              "hasErrorHandling": true,
              "context": ", ')}\n- Components: ${uiSpec.components.map(c => `${c.type}(${c.id})`).join(', ')}\n- Data Schema: ${JSON.stringify(uiSpec.dataSchema)}\n\nUser Request: ${prompt}\n\n# Output Format\nOutput ONLY one tagged block:\n<AI_HTML>\n<!DOCTYPE html>\n<html>\n<head>\n  <meta charset=\"UTF-8\">\n  <meta name=\"viewport\" cont"
            }
          ]
        },
        {
          "file": "store/canvas-store.ts",
          "issues": [
            {
              "type": "JSON.parse",
              "line": 321,
              "hasErrorHandling": true,
              "context": "   let initialState: CanvasState;\n      \n      if (stored) {\n        try {\n          const parsed = JSON.parse(stored);\n          \n          // 自动迁移旧模型配置到新模型\n          let migratedAIConfig = parsed.aiConfig || initialAIConfig;\n          const oldModel = 'gpt-5-2025-08-07';\n          const oldModel2 "
            },
            {
              "type": "JSON.parse",
              "line": 1428,
              "hasErrorHandling": true,
              "context": "          if (!value) return null;\n            \n            // 尝试解析 JSON\n            const parsed = JSON.parse(value);\n            \n            // 验证数据结构，并为节点补全 width/height（避免部署环境连线脱节）\n            if (parsed && typeof parsed === 'object' && 'state' in parsed) {\n              const state = parsed.st"
            },
            {
              "type": "JSON.stringify",
              "line": 1459,
              "hasErrorHandling": true,
              "context": ";\n            // Zustand persist 会传递 { state: {...}, version: 0 } 格式\n            const serialized = JSON.stringify(value);\n            window.localStorage.setItem(name, serialized);\n          } catch (error) {\n            console.warn('Failed to set item to localStorage:', error);\n            // 如果存"
            },
            {
              "type": "JSON.stringify",
              "line": 1467,
              "hasErrorHandling": true,
              "context": "            window.localStorage.removeItem(name);\n                window.localStorage.setItem(name, JSON.stringify(value));\n              }\n            } catch (e) {\n              console.error('Failed to recover localStorage:', e);\n            }\n          }\n        },\n        removeItem: (name: str"
            }
          ]
        },
        {
          "file": "utils/html-config-extractor.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 77,
              "hasErrorHandling": false,
              "context": "0) {\n    config.theme.extend.boxShadow = customShadows;\n  }\n  \n  return `window.tailwind.config = ${JSON.stringify(config, null, 2)};`;\n}\n\n\n\n"
            }
          ]
        },
        {
          "file": "utils/html-to-jsx-transmuter.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 61,
              "hasErrorHandling": false,
              "context": "])/g, (_, letter) => letter.toUpperCase());\n      styles[camelKey] = value;\n    }\n  });\n  \n  return JSON.stringify(styles);\n}\n\n/**\n * Transmutes raw HTML to valid JSX code string\n * \n * @param html - Raw HTML string\n * @returns Object with jsx code string and config string\n */\nexport function transm"
            }
          ]
        },
        {
          "file": "utils/html-to-react-adapter.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 330,
              "hasErrorHandling": true,
              "context": "sembly)\n  // 使用亮色背景容器\n  const styleTag = styles \n    ? `<style dangerouslySetInnerHTML={{ __html: ${JSON.stringify(styles)} }} />`\n    : '';\n\n  const componentCode = `\n    ${tailwindConfigScript ? `(() => { ${tailwindConfigScript} })();` : ''}\n\n    const App_LightMode = () => {\n      return (\n      "
            }
          ]
        },
        {
          "file": "utils/prdGenerator.ts",
          "issues": [
            {
              "type": "JSON.stringify",
              "line": 1128,
              "hasErrorHandling": true,
              "context": "🔧 [mountComponents] Starting component mounting...');\n          \n          const nodesWithCode = ${JSON.stringify(\n            data.nodes\n              .map((node, nodeIdx) => ({ node, nodeIdx }))\n              .filter(({ node }) => node.uiCode && node.uiCode.trim() && !isPlaceholderUiCode(node.uiC"
            }
          ]
        }
      ]
    },
    "networkErrorIssues": {
      "count": 10,
      "entries": [
        {
          "file": "app/actions/generate-graph.ts",
          "issues": [
            {
              "line": 679,
              "match": "429",
              "context": "uccess: aiCallSuccess,\n      errorType: aiError?.type,\n      llmMetrics,\n    });\n\n    // 如果 AI 调用失败（429/网络错误/解析错误）\n    if (!aiCallSuccess && aiError) {\n      // ✅ Rate Limit 错误：直接返回错误，不使用降级图\n      if (aiError.type === 'RATE_LIMIT') {\n        const t2_end = Date.now();\n        return {\n          type"
            },
            {
              "line": 567,
              "match": "RATE_LIMIT",
              "context": "eof SingleCallResultSchema> | null = null;\n    let aiCallSuccess = false;\n    let aiError: { type: 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE'; message: string; cooldownSeconds?: number } | null = null;\n\n    // 合并系统提示和用户提示\n    const fullPrompt = `${systemPrompt}\\n\\n${userPrompt}`;\n\n    log('⏱️ ["
            },
            {
              "line": 600,
              "match": "RATE_LIMIT",
              "context": "e: llmResult.type,\n        message: llmResult.message,\n        cooldownSeconds: llmResult.type === 'RATE_LIMIT' ? llmResult.cooldownSeconds : undefined,\n      };\n    } else {\n      // Parse JSON from response using safeParseZodJson (可恢复解析)\n      // 确保 llmResult.data 是 string 类型\n      const responseT"
            },
            {
              "line": 681,
              "match": "Rate Limit",
              "context": "mMetrics,\n    });\n\n    // 如果 AI 调用失败（429/网络错误/解析错误）\n    if (!aiCallSuccess && aiError) {\n      // ✅ Rate Limit 错误：直接返回错误，不使用降级图\n      if (aiError.type === 'RATE_LIMIT') {\n        const t2_end = Date.now();\n        return {\n          type: 'rate_limit',\n          message: aiError.message,\n          c"
            },
            {
              "line": 682,
              "match": "RATE_LIMIT",
              "context": "if (!aiCallSuccess && aiError) {\n      // ✅ Rate Limit 错误：直接返回错误，不使用降级图\n      if (aiError.type === 'RATE_LIMIT') {\n        const t2_end = Date.now();\n        return {\n          type: 'rate_limit',\n          message: aiError.message,\n          cooldownSeconds: aiError.cooldownSeconds,\n        };\n    "
            },
            {
              "line": 685,
              "match": "rate_limit",
              "context": "Error.type === 'RATE_LIMIT') {\n        const t2_end = Date.now();\n        return {\n          type: 'rate_limit',\n          message: aiError.message,\n          cooldownSeconds: aiError.cooldownSeconds,\n        };\n      }\n      \n      // 其他错误：使用降级图\n      logWarn('⚠️ [generateGraph] AI 调用失败，使用降级图', {\n "
            }
          ]
        },
        {
          "file": "app/actions/node-operations.ts",
          "issues": [
            {
              "line": 26,
              "match": "rate_limit",
              "context": "esktop' }\n  | { type: 'skeleton'; code: string; requestId?: string; stages?: unknown }\n  | { type: 'rate_limit'; cooldownSeconds: number; requestId?: string; message?: string }\n  | { type: 'network_error'; requestId?: string; message?: string; retryable?: boolean }\n  | { type: 'api_error'; requestId"
            },
            {
              "line": 191,
              "match": "RATE_LIMIT",
              "context": "nerateSpecResult = \n  | { ok: true; title: string; requirements: string[] }\n  | { ok: false; type: 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE'; message: string; cooldownSeconds?: number };\n\nexport async function reverseGenerateSpec(input: {\n  code: string;\n  nodeLabel: string;\n  projectMeta?: {\n"
            },
            {
              "line": 894,
              "match": "RATE_LIMIT",
              "context": "     });\n        \n        // Map AIResult error types to return format\n        if (result.type === 'RATE_LIMIT') {\n            return {\n              type: 'rate_limit' as const,\n            cooldownSeconds: result.cooldownSeconds || 10,\n              requestId,\n            message: result.message,\n"
            },
            {
              "line": 896,
              "match": "rate_limit",
              "context": "return format\n        if (result.type === 'RATE_LIMIT') {\n            return {\n              type: 'rate_limit' as const,\n            cooldownSeconds: result.cooldownSeconds || 10,\n              requestId,\n            message: result.message,\n          };\n        }\n        if (result.type === 'NETWO"
            },
            {
              "line": 1156,
              "match": "RATE_LIMIT",
              "context": "     });\n        \n        // Map AIResult error types to return format\n        if (result.type === 'RATE_LIMIT') {\n          return {\n            type: 'rate_limit' as const,\n            cooldownSeconds: result.cooldownSeconds || 10,\n            requestId,\n            message: result.message,\n      "
            },
            {
              "line": 1158,
              "match": "rate_limit",
              "context": " to return format\n        if (result.type === 'RATE_LIMIT') {\n          return {\n            type: 'rate_limit' as const,\n            cooldownSeconds: result.cooldownSeconds || 10,\n            requestId,\n            message: result.message,\n          };\n        }\n        if (result.type === 'NETWORK"
            },
            {
              "line": 1514,
              "match": "RATE_LIMIT",
              "context": "g: input.aiConfig,\n      });\n\n      if (!result.ok) {\n        const errorMessage = result.type === 'RATE_LIMIT' \n          ? `请求过多，请在 ${result.cooldownSeconds || 10} 秒后重试`\n          : result.message || 'PRD生成失败';\n        throw new Error(errorMessage);\n      }\n\n      // 提取生成的 Markdown\n      let markd"
            }
          ]
        },
        {
          "file": "app/actions/ui-pipeline-response.test.ts",
          "issues": [
            {
              "line": 61,
              "match": "RATE_LIMIT",
              "context": "IPipelineResponse (error)', () => {\n  const valid: UIPipelineResponse = {\n    ok: false,\n    type: 'RATE_LIMIT',\n    message: 'Too many requests',\n    cooldownSeconds: 10,\n    retryable: true,\n  };\n\n  const result = normalizeUIPipelineResponse(valid);\n  assertEqual(result, valid);\n  assertEqual(resu"
            },
            {
              "line": 70,
              "match": "RATE_LIMIT",
              "context": "(valid);\n  assertEqual(result, valid);\n  assertEqual(result.ok, false);\n  assertEqual(result.type, 'RATE_LIMIT');\n});\n\ntest('normalizeUIPipelineResponse: zsa-react tuple [data, null] (success case)', () => {\n  const data: UIPipelineResponse = {\n    ok: true,\n    type: 'UI_HTML',\n    stage: 'BEAUTIFY"
            },
            {
              "line": 112,
              "match": "Rate limit",
              "context": "elineResponse: zsa-react tuple [undefined, Error] (error case)', () => {\n  const error = new Error('Rate limit exceeded');\n  (error as any).cooldownSeconds = 15;\n  const tuple: [undefined, Error] = [undefined, error];\n  const result = normalizeUIPipelineResponse(tuple);\n\n  assertEqual(result.ok, fal"
            },
            {
              "line": 118,
              "match": "RATE_LIMIT",
              "context": "= normalizeUIPipelineResponse(tuple);\n\n  assertEqual(result.ok, false);\n  assertEqual(result.type, 'RATE_LIMIT');\n  if (!result.ok) {\n    assertEqual(result.message, 'Rate limit exceeded');\n    assertEqual(result.cooldownSeconds, 15);\n    assertEqual(result.retryable, true);\n  }\n});\n\ntest('normalize"
            },
            {
              "line": 120,
              "match": "Rate limit",
              "context": "se);\n  assertEqual(result.type, 'RATE_LIMIT');\n  if (!result.ok) {\n    assertEqual(result.message, 'Rate limit exceeded');\n    assertEqual(result.cooldownSeconds, 15);\n    assertEqual(result.retryable, true);\n  }\n});\n\ntest('normalizeUIPipelineResponse: zsa-react tuple [data, Error] (error takes prec"
            },
            {
              "line": 62,
              "match": "Too many requests",
              "context": " () => {\n  const valid: UIPipelineResponse = {\n    ok: false,\n    type: 'RATE_LIMIT',\n    message: 'Too many requests',\n    cooldownSeconds: 10,\n    retryable: true,\n  };\n\n  const result = normalizeUIPipelineResponse(valid);\n  assertEqual(result, valid);\n  assertEqual(result.ok, false);\n  assertEqua"
            }
          ]
        },
        {
          "file": "app/actions/ui-pipeline-response.ts",
          "issues": [
            {
              "line": 28,
              "match": "RATE_LIMIT",
              "context": " string;\n      meta: HTMLMeta;\n      warnings?: string[];\n    }\n  | {\n      ok: false;\n      type: 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE' | 'VALIDATION';\n      message: string;\n      cooldownSeconds?: number;\n      retryable: boolean;\n    };\n\n/**\n * Map AIResult to UIPipelineResponse (error"
            },
            {
              "line": 42,
              "match": "RATE_LIMIT",
              "context": "tage\n): UIPipelineResponse {\n  // Map AI error types to UIPipeline error types\n  let pipelineType: 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE' | 'VALIDATION' = 'PROVIDER';\n  let retryable = true;\n\n  if ('type' in aiResult) {\n    switch (aiResult.type) {\n      case 'RATE_LIMIT':\n        pipelineT"
            },
            {
              "line": 47,
              "match": "RATE_LIMIT",
              "context": "DER';\n  let retryable = true;\n\n  if ('type' in aiResult) {\n    switch (aiResult.type) {\n      case 'RATE_LIMIT':\n        pipelineType = 'RATE_LIMIT';\n        retryable = true;\n        break;\n      case 'NETWORK':\n        pipelineType = 'NETWORK';\n        retryable = true;\n        break;\n      case '"
            },
            {
              "line": 48,
              "match": "RATE_LIMIT",
              "context": "'type' in aiResult) {\n    switch (aiResult.type) {\n      case 'RATE_LIMIT':\n        pipelineType = 'RATE_LIMIT';\n        retryable = true;\n        break;\n      case 'NETWORK':\n        pipelineType = 'NETWORK';\n        retryable = true;\n        break;\n      case 'PROVIDER':\n        pipelineType = 'PR"
            },
            {
              "line": 92,
              "match": "RATE_LIMIT",
              "context": " it's already in the correct format\n    if ('ok' in raw && (raw.type === 'UI_HTML' || raw.type === 'RATE_LIMIT' || raw.type === 'NETWORK' || raw.type === 'PROVIDER' || raw.type === 'PARSE' || raw.type === 'VALIDATION')) {\n      return raw as UIPipelineResponse;\n    }\n    \n    // Case 3: Wrapped form"
            },
            {
              "line": 113,
              "match": "RATE_LIMIT",
              "context": "ldownSeconds;\n      \n      // Determine error type from error message or structure\n      let type: 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE' | 'VALIDATION' = 'PROVIDER';\n      if (errorMessage.toLowerCase().includes('rate') || errorMessage.toLowerCase().includes('limit')) {\n        type = 'RAT"
            },
            {
              "line": 115,
              "match": "RATE_LIMIT",
              "context": "e.toLowerCase().includes('rate') || errorMessage.toLowerCase().includes('limit')) {\n        type = 'RATE_LIMIT';\n      } else if (errorMessage.toLowerCase().includes('network') || errorMessage.toLowerCase().includes('fetch')) {\n        type = 'NETWORK';\n      }\n      \n      return {\n        ok: fals"
            },
            {
              "line": 125,
              "match": "RATE_LIMIT",
              "context": "\n        type,\n        message: errorMessage,\n        cooldownSeconds,\n        retryable: type === 'RATE_LIMIT' || type === 'NETWORK',\n      };\n    }\n    \n    // No error, normalize the data\n    if (data) {\n      return normalizeUIPipelineResponse(data);\n    }\n  }\n  \n  // Fallback: Invalid response "
            }
          ]
        },
        {
          "file": "components/canvas/CommandBar.tsx",
          "issues": [
            {
              "line": 540,
              "match": "429",
              "context": "e if (error.message.includes('quota') || error.message.includes('QUOTA') || error.message.includes('429')) {\n          errorMessage = 'API 配额已用完，请稍后重试';\n        } else if (error.message.includes('invalid') || error.message.includes('INVALID') || error.message.includes('400')) {\n          errorMessag"
            },
            {
              "line": 769,
              "match": "429",
              "context": "e if (error.message.includes('quota') || error.message.includes('QUOTA') || error.message.includes('429')) {\n          errorMessage = '📊 API 配额已用完\\n\\n请检查 API 配额，或稍后重试';\n        } else if (error.message.includes('invalid') || error.message.includes('INVALID') || error.message.includes('400')) {\n    "
            },
            {
              "line": 1556,
              "match": "rate_limit",
              "context": "ult) {\n              const response = uiResult;\n              \n              if (response.type === 'rate_limit') {\n                toast.error('请求过多', {\n                  description: `请在 ${response.cooldownSeconds} 秒后重试`,\n                  duration: 5000,\n                });\n                forceRe"
            },
            {
              "line": 2550,
              "match": "rate_limit",
              "context": "e all response types using discriminated union\n          switch (response.type) {\n            case 'rate_limit': {\n              toast.error('请求过多', {\n                description: `请在 ${response.cooldownSeconds} 秒后重试`,\n                duration: Math.max(response.cooldownSeconds * 1000, 5000),\n      "
            },
            {
              "line": 2723,
              "match": "isNetworkError",
              "context": "ror);\n          const msg = error instanceof Error ? error.message : String(error);\n          const isNetworkError = msg.includes('Failed to fetch') || msg.includes('fetch');\n          const displayMsg = isNetworkError\n            ? '网络连接失败。请确保服务器正在运行 (npm run dev) 并检查网络与防火墙'\n            : msg || '未"
            },
            {
              "line": 2724,
              "match": "isNetworkError",
              "context": "tworkError = msg.includes('Failed to fetch') || msg.includes('fetch');\n          const displayMsg = isNetworkError\n            ? '网络连接失败。请确保服务器正在运行 (npm run dev) 并检查网络与防火墙'\n            : msg || '未知错误';\n          updateLastAssistantMessage(displayMsg, 'error');\n          toast.error('UI代码生成失败', {\n   "
            },
            {
              "line": 2730,
              "match": "isNetworkError",
              "context": "r');\n          toast.error('UI代码生成失败', {\n            description: displayMsg,\n            duration: isNetworkError ? 8000 : 5000,\n          });\n          // 生成失败后重置状态，允许用户重试\n          // 清除UI生成专用的超时定时器\n          if (timeoutRef.current) {\n            clearTimeout(timeoutRef.current);\n            time"
            },
            {
              "line": 2968,
              "match": "isNetworkError",
              "context": ", error);\n        const msg = error instanceof Error ? error.message : String(error);\n        const isNetworkError = msg.includes('Failed to fetch') || msg.includes('fetch');\n        const description = isNetworkError\n          ? '请检查：1. 服务器是否正常运行 (npm run dev) 2. 网络连接 3. 防火墙设置'\n          : msg;\n   "
            },
            {
              "line": 2969,
              "match": "isNetworkError",
              "context": "etworkError = msg.includes('Failed to fetch') || msg.includes('fetch');\n        const description = isNetworkError\n          ? '请检查：1. 服务器是否正常运行 (npm run dev) 2. 网络连接 3. 防火墙设置'\n          : msg;\n        toast.error('生成图表失败', { description, duration: 8000 });\n        setIsProcessingVideo(false);\n     "
            }
          ]
        },
        {
          "file": "lib/ai/llm.ts",
          "issues": [
            {
              "line": 7,
              "match": "429",
              "context": "Retries: 0 (no SDK retries)\n * - AbortSignal timeout for fast-fail\n * - In-memory cooldown gate for 429\n */\n\nimport { generateText, generateObject } from 'ai';\nimport { getOpenAIClient } from '@/lib/openai-client';\nimport { log, logError, logWarn } from '@/lib/logger';\nimport { runQueued, stableHash"
            },
            {
              "line": 50,
              "match": "429",
              "context": "  }\n  return { blocked: false, cooldownSeconds: 0 };\n}\n\n/**\n * Update cooldown gate after receiving 429\n */\nfunction updateCooldownGate(cooldownSeconds: number): void {\n  rateLimitUntilMs = Date.now() + cooldownSeconds * 1000;\n  log('🚫 [LLM Gateway] Cooldown gate updated', {\n    cooldownSeconds,\n  "
            },
            {
              "line": 91,
              "match": "429",
              "context": "age = error instanceof Error ? error.message : String(error);\n  return (\n    errorMessage.includes('429') ||\n    errorMessage.includes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    ('statusCode' in error && (error as { statusCode: unknown }).statusCode === 429) ||\n    ('stat"
            },
            {
              "line": 94,
              "match": "429",
              "context": "es('rate limit') ||\n    ('statusCode' in error && (error as { statusCode: unknown }).statusCode === 429) ||\n    ('status' in error && (error as { status: unknown }).status === 429) ||\n    ('type' in error && (error as { type: unknown }).type === 'rate_limit')\n  );\n}\n\n/**\n * Check if error is network"
            },
            {
              "line": 95,
              "match": "429",
              "context": "known }).statusCode === 429) ||\n    ('status' in error && (error as { status: unknown }).status === 429) ||\n    ('type' in error && (error as { type: unknown }).type === 'rate_limit')\n  );\n}\n\n/**\n * Check if error is network error\n */\nfunction isNetworkError(error: unknown): boolean {\n  if (!error |"
            },
            {
              "line": 314,
              "match": "429",
              "context": "message: `请求超时（${timeoutMs}ms）`,\n          raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('\ud83d"
            },
            {
              "line": 589,
              "match": "429",
              "context": "message: `请求超时（${timeoutMs}ms）`,\n          raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('\ud83d"
            },
            {
              "line": 20,
              "match": "rate_limit",
              "context": "ssage } from 'ai';\nimport type { z } from 'zod';\n\n/**\n * AI Error Type (统一错误类型)\n * 全项目只使用这套类型，不再出现 'rate_limit'/'network_error' 等小写分支\n */\nexport type AIErrorType = 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE';\n\n/**\n * AI Result (Discriminated Union)\n * 统一的 LLM 调用返回类型\n */\nexport type AIResult<T> ="
            },
            {
              "line": 22,
              "match": "RATE_LIMIT",
              "context": " Type (统一错误类型)\n * 全项目只使用这套类型，不再出现 'rate_limit'/'network_error' 等小写分支\n */\nexport type AIErrorType = 'RATE_LIMIT' | 'NETWORK' | 'PROVIDER' | 'PARSE';\n\n/**\n * AI Result (Discriminated Union)\n * 统一的 LLM 调用返回类型\n */\nexport type AIResult<T> =\n  | { ok: true; data: T; raw?: string; metrics: { queuedMs: numb"
            },
            {
              "line": 33,
              "match": "rate limit",
              "context": "r; raw?: string; metrics: { queuedMs: number; dedupHit: boolean; totalMs: number } };\n\n/**\n * Local rate limit cooldown gate (in-memory)\n */\nlet rateLimitUntilMs = 0;\n\n/**\n * Check if rate limit cooldown gate is active\n */\nfunction checkCooldownGate(): { blocked: boolean; cooldownSeconds: number } {"
            },
            {
              "line": 35,
              "match": "rateLimit",
              "context": " dedupHit: boolean; totalMs: number } };\n\n/**\n * Local rate limit cooldown gate (in-memory)\n */\nlet rateLimitUntilMs = 0;\n\n/**\n * Check if rate limit cooldown gate is active\n */\nfunction checkCooldownGate(): { blocked: boolean; cooldownSeconds: number } {\n  const now = Date.now();\n  if (now < rateLi"
            },
            {
              "line": 38,
              "match": "rate limit",
              "context": ";\n\n/**\n * Local rate limit cooldown gate (in-memory)\n */\nlet rateLimitUntilMs = 0;\n\n/**\n * Check if rate limit cooldown gate is active\n */\nfunction checkCooldownGate(): { blocked: boolean; cooldownSeconds: number } {\n  const now = Date.now();\n  if (now < rateLimitUntilMs) {\n    const remainingSecond"
            },
            {
              "line": 42,
              "match": "rateLimit",
              "context": "oldownGate(): { blocked: boolean; cooldownSeconds: number } {\n  const now = Date.now();\n  if (now < rateLimitUntilMs) {\n    const remainingSeconds = Math.ceil((rateLimitUntilMs - now) / 1000);\n    return { blocked: true, cooldownSeconds: remainingSeconds };\n  }\n  return { blocked: false, cooldownSec"
            },
            {
              "line": 43,
              "match": "rateLimit",
              "context": "{\n  const now = Date.now();\n  if (now < rateLimitUntilMs) {\n    const remainingSeconds = Math.ceil((rateLimitUntilMs - now) / 1000);\n    return { blocked: true, cooldownSeconds: remainingSeconds };\n  }\n  return { blocked: false, cooldownSeconds: 0 };\n}\n\n/**\n * Update cooldown gate after receiving 42"
            },
            {
              "line": 53,
              "match": "rateLimit",
              "context": "ooldown gate after receiving 429\n */\nfunction updateCooldownGate(cooldownSeconds: number): void {\n  rateLimitUntilMs = Date.now() + cooldownSeconds * 1000;\n  log('🚫 [LLM Gateway] Cooldown gate updated', {\n    cooldownSeconds,\n    untilMs: rateLimitUntilMs,\n  });\n}\n\n/**\n * Extract cooldown seconds f"
            },
            {
              "line": 56,
              "match": "rateLimit",
              "context": "Seconds * 1000;\n  log('🚫 [LLM Gateway] Cooldown gate updated', {\n    cooldownSeconds,\n    untilMs: rateLimitUntilMs,\n  });\n}\n\n/**\n * Extract cooldown seconds from error\n */\nfunction extractCooldownSeconds(error: unknown): number {\n  if (error && typeof error === 'object') {\n    if ('response' in er"
            },
            {
              "line": 85,
              "match": "rate limit",
              "context": ": number }).cooldownSeconds;\n    }\n  }\n  return 10; // Default cooldown\n}\n\n/**\n * Check if error is rate limit error\n */\nfunction isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  const errorMessage = error instanceof Error ? error.message : Strin"
            },
            {
              "line": 87,
              "match": "RateLimit",
              "context": " }\n  }\n  return 10; // Default cooldown\n}\n\n/**\n * Check if error is rate limit error\n */\nfunction isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  const errorMessage = error instanceof Error ? error.message : String(error);\n  return (\n    errorMe"
            },
            {
              "line": 93,
              "match": "rate limit",
              "context": "age.includes('429') ||\n    errorMessage.includes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    ('statusCode' in error && (error as { statusCode: unknown }).statusCode === 429) ||\n    ('status' in error && (error as { status: unknown }).status === 429) ||\n    ('type' in error"
            },
            {
              "line": 96,
              "match": "rate_limit",
              "context": "tatus: unknown }).status === 429) ||\n    ('type' in error && (error as { type: unknown }).type === 'rate_limit')\n  );\n}\n\n/**\n * Check if error is network error\n */\nfunction isNetworkError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  const errorMessage = error"
            },
            {
              "line": 204,
              "match": "RATE_LIMIT",
              "context": " cooldownSeconds: cooldownCheck.cooldownSeconds,\n    });\n    return {\n      ok: false,\n      type: 'RATE_LIMIT',\n      message: `请求过多，请在 ${cooldownCheck.cooldownSeconds} 秒后重试`,\n      cooldownSeconds: cooldownCheck.cooldownSeconds,\n      metrics: {\n        queuedMs: 0,\n        dedupHit: false,\n      "
            },
            {
              "line": 314,
              "match": "rate limit",
              "context": ",\n          message: `请求超时（${timeoutMs}ms）`,\n          raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n       "
            },
            {
              "line": 315,
              "match": "RateLimit",
              "context": "    raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('🚫 [LLM Gateway] Rate limit error', {\n  "
            },
            {
              "line": 319,
              "match": "Rate limit",
              "context": "ds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('🚫 [LLM Gateway] Rate limit error', {\n          requestId,\n          model,\n          cooldownSeconds,\n          elapsedMs: elapsed,\n        });\n        \n        throw {\n          type: 'RATE_LIMIT',\n          message"
            },
            {
              "line": 327,
              "match": "RATE_LIMIT",
              "context": "ooldownSeconds,\n          elapsedMs: elapsed,\n        });\n        \n        throw {\n          type: 'RATE_LIMIT',\n          message: `请求过多，请在 ${cooldownSeconds} 秒后重试`,\n          cooldownSeconds,\n          raw: error instanceof Error ? error.message : String(error),\n        };\n      }\n\n      // Handle"
            },
            {
              "line": 490,
              "match": "RATE_LIMIT",
              "context": " cooldownSeconds: cooldownCheck.cooldownSeconds,\n    });\n    return {\n      ok: false,\n      type: 'RATE_LIMIT',\n      message: `请求过多，请在 ${cooldownCheck.cooldownSeconds} 秒后重试`,\n      cooldownSeconds: cooldownCheck.cooldownSeconds,\n      metrics: {\n        queuedMs: 0,\n        dedupHit: false,\n      "
            },
            {
              "line": 589,
              "match": "rate limit",
              "context": ",\n          message: `请求超时（${timeoutMs}ms）`,\n          raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n       "
            },
            {
              "line": 590,
              "match": "RateLimit",
              "context": "    raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('🚫 [LLM Gateway] Rate limit error', {\n  "
            },
            {
              "line": 594,
              "match": "Rate limit",
              "context": "ds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('🚫 [LLM Gateway] Rate limit error', {\n          requestId,\n          model,\n          cooldownSeconds,\n          elapsedMs: elapsed,\n        });\n        \n        throw {\n          type: 'RATE_LIMIT',\n          message"
            },
            {
              "line": 602,
              "match": "RATE_LIMIT",
              "context": "ooldownSeconds,\n          elapsedMs: elapsed,\n        });\n        \n        throw {\n          type: 'RATE_LIMIT',\n          message: `请求过多，请在 ${cooldownSeconds} 秒后重试`,\n          cooldownSeconds,\n          raw: error instanceof Error ? error.message : String(error),\n        };\n      }\n\n      // Handle"
            },
            {
              "line": 112,
              "match": "ECONNRESET",
              "context": "ssage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('ECONNRESET') ||\n    errorMessage.includes('network') ||\n    errorMessage.includes('NetworkError')\n  );\n\n  const hasNetworkCause = !!(\n    'cause' in error &&\n    error.cause &&\n    typeof error.cause "
            },
            {
              "line": 109,
              "match": "ECONNREFUSED",
              "context": "nst hasNetworkMessage = !!(\n    errorMessage.includes('fetch failed') ||\n    errorMessage.includes('ECONNREFUSED') ||\n    errorMessage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('ECONNRESET') ||\n    errorMessage.includes('network') ||\n    errorMessag"
            },
            {
              "line": 111,
              "match": "ETIMEDOUT",
              "context": "ge.includes('ECONNREFUSED') ||\n    errorMessage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('ECONNRESET') ||\n    errorMessage.includes('network') ||\n    errorMessage.includes('NetworkError')\n  );\n\n  const hasNetworkCause = !!(\n    'cause' in error &&\n"
            },
            {
              "line": 108,
              "match": "fetch failed",
              "context": " Error ? error.message : String(error);\n\n  const hasNetworkMessage = !!(\n    errorMessage.includes('fetch failed') ||\n    errorMessage.includes('ECONNREFUSED') ||\n    errorMessage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('ECONNRESET') ||\n    errorM"
            },
            {
              "line": 92,
              "match": "Too Many Requests",
              "context": ".message : String(error);\n  return (\n    errorMessage.includes('429') ||\n    errorMessage.includes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    ('statusCode' in error && (error as { statusCode: unknown }).statusCode === 429) ||\n    ('status' in error && (error as { status: "
            },
            {
              "line": 87,
              "match": "isRateLimitError",
              "context": "   }\n  }\n  return 10; // Default cooldown\n}\n\n/**\n * Check if error is rate limit error\n */\nfunction isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  const errorMessage = error instanceof Error ? error.message : String(error);\n  return (\n    error"
            },
            {
              "line": 315,
              "match": "isRateLimitError",
              "context": "      raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('🚫 [LLM Gateway] Rate limit error', {\n"
            },
            {
              "line": 590,
              "match": "isRateLimitError",
              "context": "      raw: '',\n        };\n      }\n\n      // Handle rate limit (429) - wrap as typed error\n      if (isRateLimitError(error)) {\n        const cooldownSeconds = extractCooldownSeconds(error);\n        updateCooldownGate(cooldownSeconds);\n        \n        logError('🚫 [LLM Gateway] Rate limit error', {\n"
            },
            {
              "line": 103,
              "match": "isNetworkError",
              "context": "type: unknown }).type === 'rate_limit')\n  );\n}\n\n/**\n * Check if error is network error\n */\nfunction isNetworkError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  const errorMessage = error instanceof Error ? error.message : String(error);\n\n  const hasNetworkMes"
            },
            {
              "line": 335,
              "match": "isNetworkError",
              "context": ": String(error),\n        };\n      }\n\n      // Handle network errors - wrap as typed error\n      if (isNetworkError(error)) {\n        logError('🌐 [LLM Gateway] Network error', {\n          requestId,\n          model,\n          error: error instanceof Error ? error.message : String(error),\n          e"
            },
            {
              "line": 610,
              "match": "isNetworkError",
              "context": ": String(error),\n        };\n      }\n\n      // Handle network errors - wrap as typed error\n      if (isNetworkError(error)) {\n        logError('🌐 [LLM Gateway] Network error', {\n          requestId,\n          model,\n          error: error instanceof Error ? error.message : String(error),\n          e"
            }
          ]
        },
        {
          "file": "lib/graph/fallback-graph.ts",
          "issues": [
            {
              "line": 4,
              "match": "rate-limit",
              "context": "/**\n * Deterministic Fallback Graph Builder\n * \n * Rule-based graph generation when AI fails or rate-limited.\n * Always returns valid schema with non-empty nodes.\n */\n\nimport type { FractalNode } from '@/types/fractal';\nimport type { Edge } from 'reactflow';\n\nexport interface FallbackGraphResult"
            }
          ]
        },
        {
          "file": "lib/openai-client.ts",
          "issues": [
            {
              "line": 113,
              "match": "429",
              "context": "| 0) + 1;\n      }\n      \n      const response = await fetch(url, fetchOptions);\n\n      // Check for 429 Rate Limit - pass through to llm.ts for handling\n      // llm.ts is the single source of truth for cooldown gate\n      if (response.status === 429) {\n        if (timeoutId) clearTimeout(timeoutId)"
            },
            {
              "line": 115,
              "match": "429",
              "context": "dling\n      // llm.ts is the single source of truth for cooldown gate\n      if (response.status === 429) {\n        if (timeoutId) clearTimeout(timeoutId);\n        const elapsed = Date.now() - startTime;\n        \n        // Extract Retry-After header for llm.ts to use (guard: headers 可能为 null)\n      "
            },
            {
              "line": 130,
              "match": "429",
              "context": "      cooldownSeconds = parsed;\n          }\n        }\n        \n        logError('🚫 [OpenAI Client] 429 Rate Limit detected (will be handled by llm.ts)', {\n          requestId,\n          elapsedMs: elapsed,\n          cooldownSeconds,\n          retryAfter: retryAfter || 'not provided',\n        });\n  "
            },
            {
              "line": 142,
              "match": "429",
              "context": "usCode?: number;\n          cooldownSeconds?: number;\n        };\n        rateLimitError.statusCode = 429;\n        rateLimitError.cooldownSeconds = cooldownSeconds;\n        requestContexts.delete(requestId);\n        throw rateLimitError;\n      }\n\n      if (timeoutId) clearTimeout(timeoutId);\n      req"
            },
            {
              "line": 113,
              "match": "Rate Limit",
              "context": " + 1;\n      }\n      \n      const response = await fetch(url, fetchOptions);\n\n      // Check for 429 Rate Limit - pass through to llm.ts for handling\n      // llm.ts is the single source of truth for cooldown gate\n      if (response.status === 429) {\n        if (timeoutId) clearTimeout(timeoutId);\n  "
            },
            {
              "line": 130,
              "match": "Rate Limit",
              "context": "  cooldownSeconds = parsed;\n          }\n        }\n        \n        logError('🚫 [OpenAI Client] 429 Rate Limit detected (will be handled by llm.ts)', {\n          requestId,\n          elapsedMs: elapsed,\n          cooldownSeconds,\n          retryAfter: retryAfter || 'not provided',\n        });\n      "
            },
            {
              "line": 138,
              "match": "rateLimit",
              "context": "});\n        \n        // Throw error with cooldown info - llm.ts will catch and handle\n        const rateLimitError = new Error('Rate limit exceeded') as Error & {\n          statusCode?: number;\n          cooldownSeconds?: number;\n        };\n        rateLimitError.statusCode = 429;\n        rateLimitE"
            },
            {
              "line": 138,
              "match": "Rate limit",
              "context": "w error with cooldown info - llm.ts will catch and handle\n        const rateLimitError = new Error('Rate limit exceeded') as Error & {\n          statusCode?: number;\n          cooldownSeconds?: number;\n        };\n        rateLimitError.statusCode = 429;\n        rateLimitError.cooldownSeconds = coold"
            },
            {
              "line": 142,
              "match": "rateLimit",
              "context": " as Error & {\n          statusCode?: number;\n          cooldownSeconds?: number;\n        };\n        rateLimitError.statusCode = 429;\n        rateLimitError.cooldownSeconds = cooldownSeconds;\n        requestContexts.delete(requestId);\n        throw rateLimitError;\n      }\n\n      if (timeoutId) clearT"
            },
            {
              "line": 143,
              "match": "rateLimit",
              "context": "er;\n          cooldownSeconds?: number;\n        };\n        rateLimitError.statusCode = 429;\n        rateLimitError.cooldownSeconds = cooldownSeconds;\n        requestContexts.delete(requestId);\n        throw rateLimitError;\n      }\n\n      if (timeoutId) clearTimeout(timeoutId);\n      requestContexts."
            },
            {
              "line": 145,
              "match": "rateLimit",
              "context": "itError.cooldownSeconds = cooldownSeconds;\n        requestContexts.delete(requestId);\n        throw rateLimitError;\n      }\n\n      if (timeoutId) clearTimeout(timeoutId);\n      requestContexts.delete(requestId);\n      return response;\n    } catch (error) {\n      if (timeoutId) clearTimeout(timeoutId"
            },
            {
              "line": 167,
              "match": "ECONNREFUSED",
              "context": "t isNetworkError = \n        errorMessage.includes('fetch failed') ||\n        errorMessage.includes('ECONNREFUSED') ||\n        errorMessage.includes('ENOTFOUND') ||\n        errorMessage.includes('ETIMEDOUT') ||\n        errorMessage.includes('network') ||\n        errorMessage.includes('NetworkError') "
            },
            {
              "line": 169,
              "match": "ETIMEDOUT",
              "context": "des('ECONNREFUSED') ||\n        errorMessage.includes('ENOTFOUND') ||\n        errorMessage.includes('ETIMEDOUT') ||\n        errorMessage.includes('network') ||\n        errorMessage.includes('NetworkError') ||\n        (error && typeof error === 'object' && 'cause' in error && \n         error.cause && "
            },
            {
              "line": 166,
              "match": "fetch failed",
              "context": "des('bodyTimeout');\n\n      // 检测网络错误类型\n      const isNetworkError = \n        errorMessage.includes('fetch failed') ||\n        errorMessage.includes('ECONNREFUSED') ||\n        errorMessage.includes('ENOTFOUND') ||\n        errorMessage.includes('ETIMEDOUT') ||\n        errorMessage.includes('network') "
            },
            {
              "line": 165,
              "match": "isNetworkError",
              "context": ");\n      const isBodyTimeout = errorMessage.includes('bodyTimeout');\n\n      // 检测网络错误类型\n      const isNetworkError = \n        errorMessage.includes('fetch failed') ||\n        errorMessage.includes('ECONNREFUSED') ||\n        errorMessage.includes('ENOTFOUND') ||\n        errorMessage.includes('ETIMEDO"
            },
            {
              "line": 204,
              "match": "isNetworkError",
              "context": "       isBodyTimeout ? 'bodyTimeout' : \n                      'abort',\n        });\n      } else if (isNetworkError) {\n        const errorCode = errorDetails.errorCode || 'UNKNOWN';\n        logError('🌐 [OpenAI Client] 网络连接失败', {\n          ...errorDetails,\n          errorCode,\n          suggestion: '"
            },
            {
              "line": 216,
              "match": "isNetworkError",
              "context": "失败', errorDetails);\n      }\n\n      // Network error retry logic (max 1 retry with jitter)\n      if (isNetworkError) {\n        const context = requestContexts.get(requestId);\n        const attemptNumber = context?.attemptNumber || 0;\n        \n        if (attemptNumber < 2) {\n          // First attemp"
            }
          ]
        },
        {
          "file": "lib/ui-generation-helpers.ts",
          "issues": [
            {
              "line": 94,
              "match": "429",
              "context": "etryAfter;\n    }\n  }\n  \n  return 10; // Default cooldown\n}\n\n/**\n * Checks if error is a rate limit (429) error\n */\nexport function isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  \n  const errorMessage = error instanceof Error ? error.message : S"
            },
            {
              "line": 102,
              "match": "429",
              "context": " = error instanceof Error ? error.message : String(error);\n  \n  return (\n    errorMessage.includes('429') ||\n    errorMessage.includes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    errorMessage.includes('Rate limit') ||\n    ('statusCode' in error && error.statusCode === 429)"
            },
            {
              "line": 106,
              "match": "429",
              "context": "') ||\n    errorMessage.includes('Rate limit') ||\n    ('statusCode' in error && error.statusCode === 429) ||\n    ('status' in error && error.status === 429)\n  );\n}\n\n/**\n * Checks if error is a network error (retryable)\n */\nexport function isNetworkError(error: unknown): boolean {\n  if (!error || type"
            },
            {
              "line": 107,
              "match": "429",
              "context": "  ('statusCode' in error && error.statusCode === 429) ||\n    ('status' in error && error.status === 429)\n  );\n}\n\n/**\n * Checks if error is a network error (retryable)\n */\nexport function isNetworkError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  \n  const err"
            },
            {
              "line": 6,
              "match": "Rate limit",
              "context": "utilities for UI generation with strict quality constraints:\n * - Precise stage timing (t0-t4)\n * - Rate limit fast-fail (<5s)\n * - Network error retry (max 1, with jitter)\n * - Output size limits\n */\n\nimport { log, logError } from './logger';\n\n/**\n * Stage timing information for performance trackin"
            },
            {
              "line": 94,
              "match": "rate limit",
              "context": "turn error.retryAfter;\n    }\n  }\n  \n  return 10; // Default cooldown\n}\n\n/**\n * Checks if error is a rate limit (429) error\n */\nexport function isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  \n  const errorMessage = error instanceof Error ? error"
            },
            {
              "line": 96,
              "match": "RateLimit",
              "context": "10; // Default cooldown\n}\n\n/**\n * Checks if error is a rate limit (429) error\n */\nexport function isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  \n  const errorMessage = error instanceof Error ? error.message : String(error);\n  \n  return (\n    e"
            },
            {
              "line": 104,
              "match": "rate limit",
              "context": "age.includes('429') ||\n    errorMessage.includes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    errorMessage.includes('Rate limit') ||\n    ('statusCode' in error && error.statusCode === 429) ||\n    ('status' in error && error.status === 429)\n  );\n}\n\n/**\n * Checks if error is "
            },
            {
              "line": 105,
              "match": "Rate limit",
              "context": "ludes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    errorMessage.includes('Rate limit') ||\n    ('statusCode' in error && error.statusCode === 429) ||\n    ('status' in error && error.status === 429)\n  );\n}\n\n/**\n * Checks if error is a network error (retryable)\n */\nexport func"
            },
            {
              "line": 198,
              "match": "RATE_LIMIT",
              "context": "S = {\n  MAX_SKELETON_HTML_LENGTH: 50_000,\n  MAX_FINAL_HTML_LENGTH: 200_000,\n  MAX_LIST_ITEMS: 50,\n  RATE_LIMIT_FAST_FAIL_MS: 5_000,\n  NETWORK_RETRY_DELAY_MS: 1_000,\n  NETWORK_RETRY_JITTER_MS: 500,\n} as const;\n\n/**\n * Wraps generateText with rate limit fast-fail and network retry logic\n * \n * @param "
            },
            {
              "line": 204,
              "match": "rate limit",
              "context": "_RETRY_DELAY_MS: 1_000,\n  NETWORK_RETRY_JITTER_MS: 500,\n} as const;\n\n/**\n * Wraps generateText with rate limit fast-fail and network retry logic\n * \n * @param generateTextFn - Function that returns a Promise<{ text: string }>\n * @param requestId - Request ID for logging\n * @param model - Model name "
            },
            {
              "line": 222,
              "match": "Rate limit",
              "context": "try {\n    // First attempt\n    const result = await Promise.race([\n      generateTextFn(),\n      // Rate limit fast-fail timeout\n      new Promise<never>((_, reject) => {\n        setTimeout(() => {\n          reject(new Error('Rate limit check timeout'));\n        }, UI_GENERATION_CONSTANTS.RATE_LIMIT"
            },
            {
              "line": 225,
              "match": "Rate limit",
              "context": "ut\n      new Promise<never>((_, reject) => {\n        setTimeout(() => {\n          reject(new Error('Rate limit check timeout'));\n        }, UI_GENERATION_CONSTANTS.RATE_LIMIT_FAST_FAIL_MS);\n      }),\n    ]);\n    \n    return result;\n  } catch (error) {\n    const elapsed = Date.now() - startTime;\n    "
            },
            {
              "line": 226,
              "match": "RATE_LIMIT",
              "context": "() => {\n          reject(new Error('Rate limit check timeout'));\n        }, UI_GENERATION_CONSTANTS.RATE_LIMIT_FAST_FAIL_MS);\n      }),\n    ]);\n    \n    return result;\n  } catch (error) {\n    const elapsed = Date.now() - startTime;\n    \n    // Check for rate limit error\n    if (isRateLimitError(erro"
            },
            {
              "line": 234,
              "match": "rate limit",
              "context": "eturn result;\n  } catch (error) {\n    const elapsed = Date.now() - startTime;\n    \n    // Check for rate limit error\n    if (isRateLimitError(error)) {\n      const cooldown = extractCooldownSeconds(error);\n      logError('🚫 [Rate Limit] Fast-fail triggered', {\n        requestId,\n        model,\n    "
            },
            {
              "line": 235,
              "match": "RateLimit",
              "context": "ror) {\n    const elapsed = Date.now() - startTime;\n    \n    // Check for rate limit error\n    if (isRateLimitError(error)) {\n      const cooldown = extractCooldownSeconds(error);\n      logError('🚫 [Rate Limit] Fast-fail triggered', {\n        requestId,\n        model,\n        elapsedMs: elapsed,\n   "
            },
            {
              "line": 237,
              "match": "Rate Limit",
              "context": "sRateLimitError(error)) {\n      const cooldown = extractCooldownSeconds(error);\n      logError('🚫 [Rate Limit] Fast-fail triggered', {\n        requestId,\n        model,\n        elapsedMs: elapsed,\n        cooldownSeconds: cooldown,\n      });\n      \n      const rateLimitError = new Error('Rate limit"
            },
            {
              "line": 244,
              "match": "rateLimit",
              "context": " model,\n        elapsedMs: elapsed,\n        cooldownSeconds: cooldown,\n      });\n      \n      const rateLimitError = new Error('Rate limit exceeded') as Error & {\n        type: 'rate_limit';\n        cooldownSeconds: number;\n      };\n      rateLimitError.type = 'rate_limit';\n      rateLimitError.cool"
            },
            {
              "line": 244,
              "match": "Rate limit",
              "context": "lapsed,\n        cooldownSeconds: cooldown,\n      });\n      \n      const rateLimitError = new Error('Rate limit exceeded') as Error & {\n        type: 'rate_limit';\n        cooldownSeconds: number;\n      };\n      rateLimitError.type = 'rate_limit';\n      rateLimitError.cooldownSeconds = cooldown;\n    "
            },
            {
              "line": 245,
              "match": "rate_limit",
              "context": ");\n      \n      const rateLimitError = new Error('Rate limit exceeded') as Error & {\n        type: 'rate_limit';\n        cooldownSeconds: number;\n      };\n      rateLimitError.type = 'rate_limit';\n      rateLimitError.cooldownSeconds = cooldown;\n      throw rateLimitError;\n    }\n    \n    // Check fo"
            },
            {
              "line": 248,
              "match": "rateLimit",
              "context": "exceeded') as Error & {\n        type: 'rate_limit';\n        cooldownSeconds: number;\n      };\n      rateLimitError.type = 'rate_limit';\n      rateLimitError.cooldownSeconds = cooldown;\n      throw rateLimitError;\n    }\n    \n    // Check for network error (retryable)\n    if (isNetworkError(error) && "
            },
            {
              "line": 248,
              "match": "rate_limit",
              "context": "\n        type: 'rate_limit';\n        cooldownSeconds: number;\n      };\n      rateLimitError.type = 'rate_limit';\n      rateLimitError.cooldownSeconds = cooldown;\n      throw rateLimitError;\n    }\n    \n    // Check for network error (retryable)\n    if (isNetworkError(error) && elapsed < UI_GENERATION"
            },
            {
              "line": 249,
              "match": "rateLimit",
              "context": "e_limit';\n        cooldownSeconds: number;\n      };\n      rateLimitError.type = 'rate_limit';\n      rateLimitError.cooldownSeconds = cooldown;\n      throw rateLimitError;\n    }\n    \n    // Check for network error (retryable)\n    if (isNetworkError(error) && elapsed < UI_GENERATION_CONSTANTS.RATE_LIM"
            },
            {
              "line": 250,
              "match": "rateLimit",
              "context": "   rateLimitError.type = 'rate_limit';\n      rateLimitError.cooldownSeconds = cooldown;\n      throw rateLimitError;\n    }\n    \n    // Check for network error (retryable)\n    if (isNetworkError(error) && elapsed < UI_GENERATION_CONSTANTS.RATE_LIMIT_FAST_FAIL_MS) {\n      logError('🌐 [Network Error] F"
            },
            {
              "line": 254,
              "match": "RATE_LIMIT",
              "context": "ck for network error (retryable)\n    if (isNetworkError(error) && elapsed < UI_GENERATION_CONSTANTS.RATE_LIMIT_FAST_FAIL_MS) {\n      logError('🌐 [Network Error] First attempt failed, retrying with jitter', {\n        requestId,\n        model,\n        elapsedMs: elapsed,\n        retryable: true,\n    "
            },
            {
              "line": 121,
              "match": "ECONNREFUSED",
              "context": "const hasNetworkMessage = (\n    errorMessage.includes('fetch failed') ||\n    errorMessage.includes('ECONNREFUSED') ||\n    errorMessage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('network') ||\n    errorMessage.includes('NetworkError')\n  );\n  \n  const "
            },
            {
              "line": 123,
              "match": "ETIMEDOUT",
              "context": "ge.includes('ECONNREFUSED') ||\n    errorMessage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('network') ||\n    errorMessage.includes('NetworkError')\n  );\n  \n  const hasNetworkCause = Boolean(\n    'cause' in error &&\n    error.cause &&\n    typeof error."
            },
            {
              "line": 120,
              "match": "fetch failed",
              "context": " Error ? error.message : String(error);\n  \n  const hasNetworkMessage = (\n    errorMessage.includes('fetch failed') ||\n    errorMessage.includes('ECONNREFUSED') ||\n    errorMessage.includes('ENOTFOUND') ||\n    errorMessage.includes('ETIMEDOUT') ||\n    errorMessage.includes('network') ||\n    errorMess"
            },
            {
              "line": 103,
              "match": "Too Many Requests",
              "context": "ssage : String(error);\n  \n  return (\n    errorMessage.includes('429') ||\n    errorMessage.includes('Too Many Requests') ||\n    errorMessage.includes('rate limit') ||\n    errorMessage.includes('Rate limit') ||\n    ('statusCode' in error && error.statusCode === 429) ||\n    ('status' in error && error."
            },
            {
              "line": 96,
              "match": "isRateLimitError",
              "context": "n 10; // Default cooldown\n}\n\n/**\n * Checks if error is a rate limit (429) error\n */\nexport function isRateLimitError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  \n  const errorMessage = error instanceof Error ? error.message : String(error);\n  \n  return (\n   "
            },
            {
              "line": 235,
              "match": "isRateLimitError",
              "context": "error) {\n    const elapsed = Date.now() - startTime;\n    \n    // Check for rate limit error\n    if (isRateLimitError(error)) {\n      const cooldown = extractCooldownSeconds(error);\n      logError('🚫 [Rate Limit] Fast-fail triggered', {\n        requestId,\n        model,\n        elapsedMs: elapsed,\n "
            },
            {
              "line": 114,
              "match": "isNetworkError",
              "context": "r.status === 429)\n  );\n}\n\n/**\n * Checks if error is a network error (retryable)\n */\nexport function isNetworkError(error: unknown): boolean {\n  if (!error || typeof error !== 'object') return false;\n  \n  const errorMessage = error instanceof Error ? error.message : String(error);\n  \n  const hasNetwo"
            },
            {
              "line": 254,
              "match": "isNetworkError",
              "context": "cooldown;\n      throw rateLimitError;\n    }\n    \n    // Check for network error (retryable)\n    if (isNetworkError(error) && elapsed < UI_GENERATION_CONSTANTS.RATE_LIMIT_FAST_FAIL_MS) {\n      logError('🌐 [Network Error] First attempt failed, retrying with jitter', {\n        requestId,\n        model"
            }
          ]
        },
        {
          "file": "lib/user-facing-messages.ts",
          "issues": [
            {
              "line": 20,
              "match": "RATE_LIMIT",
              "context": "AIErrorType | PipelineErrorType,\n  cooldownSeconds?: number\n): string {\n  switch (type) {\n    case 'RATE_LIMIT':\n      return typeof cooldownSeconds === 'number' && cooldownSeconds > 0\n        ? `操作过于频繁，请 ${cooldownSeconds} 秒后重试`\n        : '操作过于频繁，请稍后重试';\n    case 'NETWORK':\n      return '网络异常，请检查网络"
            }
          ]
        }
      ]
    },
    "substringIssues": {
      "count": 23,
      "entries": [
        {
          "file": "app/actions/generate-graph.ts",
          "issues": [
            {
              "line": 330,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "utClarity] Tagged block not found, using fallback', {\n      requestId,\n      rawPreview: result.data.substring(0, 200),\n    });\n    return {\n      confidence: 45,\n      isVague: true,\n      detectedDomain: '',\n      detectedBusinessObject: '',\n      detectedAction: '',\n    };\n  }\n\n  // 使用 safeParseZ"
            },
            {
              "line": 364,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "', {\n      requestId,\n      error: parseResult.error,\n      rawPreview: parseResult.raw || blockText.substring(0, 200),\n    });\n    return {\n      confidence: 45,\n      isVague: true,\n      detectedDomain: '',\n      detectedBusinessObject: '',\n      detectedAction: '',\n    };\n  }\n\n  // 解析成功，使用结果\n  c"
            },
            {
              "line": 492,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "', {\n      requestId,\n        promptLength: input.prompt.length,\n        promptPreview: input.prompt.substring(0, 100),\n        hasMedia: !!input.mediaBase64,\n        mediaType: input.mediaType,\n      hasAttachment: !!input.attachmentContent,\n      });\n\n      if (!getOpenAIKey()) {\n        logError("
            },
            {
              "line": 658,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "d,\n              error: parseResult.error,\n              rawPreview: parseResult.raw || responseText.substring(0, 500),\n              llmMetrics,\n            });\n            aiError = {\n              type: 'PARSE',\n              message: `JSON 解析失败: ${parseResult.error}`,\n            };\n          }\n"
            },
            {
              "line": 214,
              "match": ".substr(",
              "hasTypeCheck": true,
              "context": "putClarityAnalysisSchema>> {\n  const requestId = `clarity-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n  \n  // 构建包含文件内容的分析内容\n  let analysisContent = prompt.trim() || '用户输入为空';\n  let hasFileContent = false;\n  \n  if (attachmentContent) {\n    analysisContent += `\\n\\n附件内容：\\n${attachmentCon"
            },
            {
              "line": 484,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "{\n    const t0 = Date.now();\n    const requestId = `graph-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n    let warnings: string[] = [];\n    let fallbackUsed = false;\n    let llmMetrics = { queuedMs: 0, dedupHit: false, totalMs: 0 };\n    \n    log('🚀 [generateGraph] 开始生成图结构（单次调用）', {\n  "
            }
          ]
        },
        {
          "file": "app/actions/node-operations.ts",
          "issues": [
            {
              "line": 323,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "f (requirements.length === 0) {\n      // 如果没有解析到需求，将整个文本作为单个需求\n      requirements.push(generatedText.substring(0, 200));\n    }\n\n    return {\n      title: title || nodeLabel,\n      requirements: requirements,\n    };\n  } catch (error) {\n    logError('❌ [reverseGenerateSpec] Error:', error);\n    const "
            },
            {
              "line": 454,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "Cases.length > 0) {\n        cases = listCases;\n      } else {\n        const fallback = generatedText.substring(0, 500);\n        cases.push(\n          fallback.length > 0\n            ? fallback\n            : '| 序号 | 测试场景 | 预期结果 |\\n| --- | --- | --- |\\n| 1 | 功能正常流程测试 | 通过 |\\n| 2 | 数据验证测试 | 通过 |\\n| 3 |"
            },
            {
              "line": 670,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " input.imageBase64.length,\n        base64Length: base64Data.length,\n        base64Prefix: base64Data.substring(0, 30),\n      });\n\n      // 构建设计系统约束（如果提供了主题配置）\n      const designSystemEnforcement = input.themeConfig\n        ? buildDesignSystemEnforcement(input.themeConfig)\n        : '';\n\n      // 获取项"
            },
            {
              "line": 844,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "teUIFromImage] 用户提示词:', {\n        promptLength: userPrompt.length,\n        promptPreview: userPrompt.substring(0, 100),\n      });\n\n      // Stitch 双模型分轨：draft=快速模型，quality=重量模型\n      const tier: GenerationTier = input.tier === 'draft' ? 'draft' : 'quality';\n      const visionModel = getModelForTier("
            },
            {
              "line": 926,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "gth: result.data.length,\n        trimmedLength: generatedCode.length,\n        preview: generatedCode.substring(0, 200),\n      });\n\n      // 清理代码：移除可能的 markdown 代码块标记\n      generatedCode = generatedCode\n        .replace(/^```(?:tsx|jsx|typescript|javascript)?\\n?/gm, '')\n        .replace(/\\n?```$/gm, "
            },
            {
              "line": 936,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "eUIFromImage] 代码清理后:', {\n        cleanedLength: generatedCode.length,\n        preview: generatedCode.substring(0, 200),\n      });\n\n      // 验证代码是否有效（不是空字符串）\n      if (!generatedCode || generatedCode.length < 50) {\n        logError('❌ [generateUIFromImage] 生成的代码太短:', {\n          codeLength: generated"
            },
            {
              "line": 1194,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "gth: result.data.length,\n        trimmedLength: generatedCode.length,\n        preview: generatedCode.substring(0, 200),\n      });\n\n      // 清理代码：移除可能的 markdown 代码块标记\n      generatedCode = generatedCode\n        .replace(/^```(?:tsx|jsx|typescript|javascript)?\\n?/gm, '')\n        .replace(/\\n?```$/gm, "
            },
            {
              "line": 1232,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "teUIFromText] 代码清理后:', {\n        cleanedLength: generatedCode.length,\n        preview: generatedCode.substring(0, 200),\n      });\n\n      // 结构自检：便于与图中预期对比，确认主内容区是否有实质内容\n      const hasFlex1 = /\\bflex-1\\b/.test(generatedCode);\n      const hasMain = /<main\\b/.test(generatedCode);\n      const listItemC"
            },
            {
              "line": 1311,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "al) {\n        logError('❌ [generateUIFromText] 模型返回了说明/提问文字而非代码', {\n          preview: generatedCode.substring(0, 300),\n        });\n        throw new Error(\n          '模型返回了说明文字而非 UI 代码。请在下拉或节点中补充页面描述（如：商品详情页、列表页、包含的模块），再点击「生成本页 UI」重试。'\n        );\n      }\n\n      // 一页完整 UI 代码通常 5000–15000 字符，过短多为骨架/"
            },
            {
              "line": 1536,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "est(markdown)) {\n        logError('❌ [generateAnalysisFromCode] 模型返回了澄清/选项而非表格', { preview: markdown.substring(0, 300) });\n        throw new Error('本次返回了说明文字而非需求表格，请直接再次点击「生成需求文档」重试；若仍出现，请稍后重试。');\n      }\n\n      // 验证生成的文档是否有效\n      if (!markdown || markdown.length < 20) {\n        throw new Error('生"
            },
            {
              "line": 626,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": " const startTime = Date.now();\n    const requestId = `req-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n    \n    log('='.repeat(80));\n    log(`🚀 [generateUIFromImage] 开始处理图片生成UI请求 [${requestId}]`);\n    log(`📋 [generateUIFromImage] 输入参数 [${requestId}]:`, {\n      promptLength: input.pro"
            },
            {
              "line": 1049,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": " const startTime = Date.now();\n    const requestId = `req-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n    \n    log('='.repeat(80));\n    log(`🚀 [generateUIFromText] 开始处理文本生成UI请求 [${requestId}]`);\n    const viewportPreset = (input.viewportPreset === 'desktop' || input.viewportPreset =="
            }
          ]
        },
        {
          "file": "app/actions/parse-topology.ts",
          "issues": [
            {
              "line": 63,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "const startTime = Date.now();\n    const requestId = `topo-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n    \n    log('='.repeat(80));\n    log(`🗺️ [parseTopology] 开始解析拓扑图 [${requestId}]`);\n    log(`📋 [parseTopology] 输入参数 [${requestId}]:`, {\n      imageBase64Length: input.imageBase64?.l"
            }
          ]
        },
        {
          "file": "components/canvas/CommandBar.tsx",
          "issues": [
            {
              "line": 2601,
              "match": "uiCode.substring",
              "hasTypeCheck": false,
              "context": "\udd0d [CommandBar] 提取的UI代码:', {\n                codeLength: uiCode.length,\n                codePreview: uiCode.substring(0, 200),\n                isValid: uiCode && uiCode.length > 50,\n              });\n              \n              if (uiCode && uiCode.length > 50) {\n                const vUsed = 'viewp"
            },
            {
              "line": 2612,
              "match": "uiCode.substring",
              "hasTypeCheck": false,
              "context": "Label: targetNodeLabel,\n                  codeLength: uiCode.length,\n                  codePreview: uiCode.substring(0, 100),\n                });\n            \n            updateNodeData(targetNodeId, {\n              artifacts: {\n                view: {\n                  code: uiCode,\n               "
            },
            {
              "line": 2634,
              "match": "uiCode.substring",
              "hasTypeCheck": false,
              "context": "       isMatch: savedCode === uiCode,\n                  codeMatches: savedCode.substring(0, 50) === uiCode.substring(0, 50),\n                });\n                \n                if (savedCode.length === 0 || savedCode === '// PLACEHOLDER') {\n                  logError('❌ [CommandBar] 节点更新失败！代码未保存:',"
            },
            {
              "line": 149,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "result as any) : [],\n        // 尝试序列化查看完整结构（限制长度避免日志过大）\n        resultString: JSON.stringify(result).substring(0, 500),\n        // 直接检查 result.data 的内容\n        dataExists: !!(result as any)?.data,\n        dataType: typeof (result as any)?.data,\n        dataKeys: (result as any)?.data && typeof (resu"
            },
            {
              "line": 164,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "/ 完整的数据结构（限制长度）\n        fullDataString: (result as any)?.data ? JSON.stringify((result as any).data).substring(0, 1000) : 'N/A',\n      });\n\n      // 处理 zsa-react 的返回格式\n      // zsa-react 的 useServerAction 返回格式：result 直接是 handler 的返回值\n      // 所以 result 应该是 { data: { type: 'clarification_needed' | 'g"
            },
            {
              "line": 236,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "   dataNodesValue: (resultData as any)?.data?.nodes ? JSON.stringify((resultData as any).data.nodes).substring(0, 200) : 'N/A',\n        dataEdgesType: typeof (resultData as any)?.data?.edges,\n        dataEdgesIsArray: Array.isArray((resultData as any)?.data?.edges),\n        dataEdgesLength: Array.is"
            },
            {
              "line": 248,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "esultData as any)?.edges),\n        // 完整结构预览（限制长度）\n        fullStructure: JSON.stringify(resultData).substring(0, 1000),\n      });\n\n      // 提取 nodes 和 edges\n      // 尝试多种可能的路径\n      let nodes: any[] | undefined;\n      let edges: any[] | undefined;\n\n      // 优先尝试 resultData.data.nodes 和 resultData.d"
            },
            {
              "line": 297,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "isArray(data.edges),\n          // 完整 data 对象的结构（限制长度）\n          fullDataString: JSON.stringify(data).substring(0, 1000),\n        });\n        \n        // 如果 data 中只有一个键，且这个键的值是对象，尝试从中提取 nodes/edges（不再处理 clarification_needed，避免无法继续）\n        if (dataKeys.length === 1 && typeof data[dataKeys[0]] === 'ob"
            },
            {
              "line": 325,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " (!nodes) {\n          const nodesStringified = data.nodes !== undefined ? JSON.stringify(data.nodes).substring(0, 200) : 'undefined';\n          logWarn('⚠️ [CommandBar] resultData.data.nodes 不是数组:', {\n            type: typeof data.nodes,\n            value: data.nodes,\n            isUndefined: data.n"
            },
            {
              "line": 339,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " (!edges) {\n          const edgesStringified = data.edges !== undefined ? JSON.stringify(data.edges).substring(0, 200) : 'undefined';\n          logWarn('⚠️ [CommandBar] resultData.data.edges 不是数组:', {\n            type: typeof data.edges,\n            value: data.edges,\n            isUndefined: data.e"
            },
            {
              "line": 1708,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "esult,\n            codeLength: codeFromResult?.length || 0,\n            codePreview: codeFromResult?.substring(0, 100),\n            dataKeys: resultData ? Object.keys(resultData) : [],\n          });\n\n          // 如果没有数据或没有 code，抛出错误\n          if (!resultData || !codeFromResult) {\n            logErro"
            },
            {
              "line": 1732,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " uiResult,\n              resultData,\n              hasCode: !!code,\n              codePreview: code?.substring(0, 100),\n              isPlaceholder: code === placeholderCode,\n            });\n            throw new Error('UI 代码生成失败：服务器返回了占位符代码。请检查 server action 实现，确保 generateUIFromImage 已正确实现 AI 生成逻辑'"
            },
            {
              "line": 1747,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "tNodeLabel,\n            codeLength: accumulatedCode.length,\n            codePreview: accumulatedCode.substring(0, 100),\n            hasPreviewUrl: !!imageForUI?.preview,\n          });\n          \n          // 从store获取最新的节点数据，确保使用最新的artifacts\n          const latestNode = useCanvasStore.getState().node"
            },
            {
              "line": 1779,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "ts?.view?.code?.length || 0,\n                savedCodePreview: savedNode.data.artifacts?.view?.code?.substring(0, 100) || 'N/A',\n                isMatch: savedNode.data.artifacts?.view?.code === accumulatedCode,\n              });\n            } else {\n              logError('❌ [CommandBar] UI代码保存验证失败"
            },
            {
              "line": 1985,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "    // 增加HTML内容长度限制到100000字符，确保完整传递（支持大型HTML文件）\n            const htmlContentForPrompt = htmlContent.substring(0, 100000);\n            const isTruncated = htmlContent.length > 100000;\n            \n            // 解析HTML提取关键信息（用于精确复刻）\n            const parsedInfo = parseHTML(htmlContent);\n            "
            },
            {
              "line": 2169,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "          promptLength: htmlReferencePrompt.length,\n              promptPreview: htmlReferencePrompt.substring(0, 200),\n              nodeLabel: targetNodeLabel,\n              targetNodeId: targetNodeId,\n              hasProjectMeta: !!useCanvasStore.getState().projectMeta,\n              hasThemeCon"
            },
            {
              "line": 2601,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "mandBar] 提取的UI代码:', {\n                codeLength: uiCode.length,\n                codePreview: uiCode.substring(0, 200),\n                isValid: uiCode && uiCode.length > 50,\n              });\n              \n              if (uiCode && uiCode.length > 50) {\n                const vUsed = 'viewportUse"
            },
            {
              "line": 2612,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " targetNodeLabel,\n                  codeLength: uiCode.length,\n                  codePreview: uiCode.substring(0, 100),\n                });\n            \n            updateNodeData(targetNodeId, {\n              artifacts: {\n                view: {\n                  code: uiCode,\n                  vie"
            },
            {
              "line": 2632,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": ",\n                  savedCodeLength: savedCode.length,\n                  savedCodePreview: savedCode.substring(0, 100),\n                  isMatch: savedCode === uiCode,\n                  codeMatches: savedCode.substring(0, 50) === uiCode.substring(0, 50),\n                });\n                \n       "
            },
            {
              "line": 2634,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "g(0, 100),\n                  isMatch: savedCode === uiCode,\n                  codeMatches: savedCode.substring(0, 50) === uiCode.substring(0, 50),\n                });\n                \n                if (savedCode.length === 0 || savedCode === '// PLACEHOLDER') {\n                  logError('❌ [Comma"
            },
            {
              "line": 2634,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " isMatch: savedCode === uiCode,\n                  codeMatches: savedCode.substring(0, 50) === uiCode.substring(0, 50),\n                });\n                \n                if (savedCode.length === 0 || savedCode === '// PLACEHOLDER') {\n                  logError('❌ [CommandBar] 节点更新失败！代码未保存:', {\n   "
            },
            {
              "line": 2823,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "rtifacts.view.previewUrl : 'undefined',\n        previewUrlPrefix: currentArtifacts.view?.previewUrl?.substring(0, 30) || 'N/A',\n      });\n\n      log('📤 [CommandBar] Calling executeUpdate with:', {\n        nodeId: selectedNode.id,\n        nodeTitle: selectedNode.data.label,\n        userPrompt: promp"
            },
            {
              "line": 2871,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "\n          // 文本文件、PDF 或 Word 文档\n          textAttachments.push(`\\n\\n【文件：${att.name}】\\n${att.content.substring(0, 50000)}`); // 限制每个文件最多 50000 字符\n          attachmentNames.push(att.name);\n          if (!attachmentType) {\n            attachmentType = att.type === 'text' ? 'text' : 'media';\n          "
            }
          ]
        },
        {
          "file": "components/canvas/HtmlFirstPreview.tsx",
          "issues": [
            {
              "line": 585,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "v className=\"w-full h-full relative\">\n            <HtmlSandbox\n              key={`edit-${htmlSource.substring(0, 50)}`} // 使用 key 强制重新渲染\n              html={htmlSource}\n              mode=\"edit\"\n              injectorScript={buildEditorInjector({ mode: 'edit' }) + '\\n' + buildInjectorScript({})}\n  "
            },
            {
              "line": 611,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "lassName=\"w-full h-full relative\">\n            <HtmlSandbox\n              key={`preview-${htmlSource.substring(0, 50)}`} // 使用 key 强制重新渲染\n              html={htmlSource}\n              mode=\"preview\"\n              injectorScript={buildInjectorScript({})}\n              heightMode=\"auto\"\n              "
            }
          ]
        },
        {
          "file": "components/canvas/MermaidDiagram.tsx",
          "issues": [
            {
              "line": 74,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "ule.default;\n        \n        // 生成唯一ID防止冲突\n        const id = `mermaid-${Math.random().toString(36).substr(2, 9)}`;\n        const { svg } = await mermaid.render(id, diagramCode);\n        setSvg(svg);\n        setError(false);\n      } catch (e) {\n        console.error('Mermaid render error:', e);\n   "
            }
          ]
        },
        {
          "file": "components/canvas/ProjectBlueprint.tsx",
          "issues": [
            {
              "line": 458,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "Stories.length > 0) {\n        const nodeLabel = (node.data.label || node.id).replace(/\"/g, '&quot;').substring(0, 30);\n        lines.push(`    section ${nodeLabel}`);\n        \n        // 为每个用户故事添加步骤\n        nodeStories.forEach((item) => {\n          const stepLabel = item.story.activity.replace(/\"/g,"
            },
            {
              "line": 463,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "eStories.forEach((item) => {\n          const stepLabel = item.story.activity.replace(/\"/g, '&quot;').substring(0, 40);\n          const satisfaction = 75; // 默认满意度\n          const role = item.story.role.replace(/\"/g, '&quot;').substring(0, 20);\n          lines.push(`      ${stepLabel}: ${satisfaction"
            },
            {
              "line": 465,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "    const satisfaction = 75; // 默认满意度\n          const role = item.story.role.replace(/\"/g, '&quot;').substring(0, 20);\n          lines.push(`      ${stepLabel}: ${satisfaction}: ${role}`);\n        });\n      }\n    });\n\n    return lines.join('\\n');\n  }, [generateUserJourneyTable, nodes]);\n\n  // 为单个用户故"
            }
          ]
        },
        {
          "file": "components/canvas/WordUploader.tsx",
          "issues": [
            {
              "line": 105,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "展名有效，继续处理:', {\n        fileName: file.name,\n        fileType: file.type,\n        extension: fileName.substring(fileName.lastIndexOf('.'))\n      });\n    }\n\n    // 验证文件大小（限制 10MB）\n    const maxSize = 10 * 1024 * 1024; // 10MB\n    if (file.size > maxSize) {\n      toast.error('文件大小不能超过 10MB');\n      ret"
            }
          ]
        },
        {
          "file": "lib/ai/gateway-queue.ts",
          "issues": [
            {
              "line": 82,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "(existingPromise) {\n    log('🔄 [Gateway Queue] Deduplication hit', {\n      provider,\n      key: key.substring(0, 50) + '...',\n    });\n\n    const { result } = await existingPromise;\n    // ✅ 计算实际等待时间（dedup 的 caller 也可能等了几百 ms / 几秒）\n    const queuedMs = Date.now() - waitStartMs;\n    return {\n      re"
            },
            {
              "line": 167,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "set(provider, queue);\n    \n    log('⏳ [Gateway Queue] Task queued', {\n      provider,\n      key: key.substring(0, 50) + '...',\n      queueLength: queue.length,\n    });\n  });\n}\n\n/**\n * Process next task in provider queue\n */\nfunction processQueue(provider: string): void {\n  const queue = providerQueu"
            },
            {
              "line": 195,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "waitStartMs;\n  \n  log('▶️ [Gateway Queue] Processing queued task', {\n    provider,\n    key: task.key.substring(0, 50) + '...',\n    queuedMs,\n  });\n\n  Promise.resolve(task.fn({ queuedMs, dedupHit: false }))\n    .then((result) => task.resolve({ result, queuedMs }))\n    .catch((error) =>\n      task.rej"
            }
          ]
        },
        {
          "file": "lib/ai/json-extract.ts",
          "issues": [
            {
              "line": 58,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "== '}') {\n      braceCount--;\n      if (braceCount === 0) {\n        // 找到完整对象\n        return trimmed.substring(startPos, i + 1);\n      }\n    }\n  }\n\n  // 未找到完整对象\n  return null;\n}\n\n/**\n * 安全解析 JSON 并验证 Zod Schema\n * 内部：extractFirstJsonObject -> JSON.parse -> schema.safeParse\n * 失败时返回 ok:false，不抛异常\n */"
            },
            {
              "line": 84,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "k: false,\n        error: 'No valid JSON object found in text',\n        raw: text.length > 200 ? text.substring(0, 200) + '...' : text,\n      };\n    }\n\n    // 步骤2: 解析 JSON\n    let parsed: unknown;\n    try {\n      parsed = JSON.parse(jsonStr);\n    } catch (parseError) {\n      return {\n        ok: fals"
            },
            {
              "line": 96,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "nceof Error ? parseError.message : String(parseError)}`,\n        raw: jsonStr.length > 200 ? jsonStr.substring(0, 200) + '...' : jsonStr,\n      };\n    }\n\n    // 步骤3: Zod 验证\n    const result = schema.safeParse(parsed);\n    \n    if (result.success) {\n      return { ok: true, data: result.data };\n    }"
            },
            {
              "line": 109,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "(e => `${e.path.join('.')}: ${e.message}`).join('; ')}`,\n        raw: jsonStr.length > 200 ? jsonStr.substring(0, 200) + '...' : jsonStr,\n      };\n    }\n  } catch (error) {\n    return {\n      ok: false,\n      error: `Unexpected error: ${error instanceof Error ? error.message : String(error)}`,\n     "
            },
            {
              "line": 116,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "ror: ${error instanceof Error ? error.message : String(error)}`,\n      raw: text.length > 200 ? text.substring(0, 200) + '...' : text,\n    };\n  }\n}\n\n"
            }
          ]
        },
        {
          "file": "lib/ai/llm.ts",
          "issues": [
            {
              "line": 184,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "otalMs: 0,\n      },\n    };\n  }\n\n  const requestId = `call-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n\n  // Generate stable task key: model + actionName + hash(prompt/messages) + hash(attachments) + mode\n  // Avoid concatenating long prompt/messages into keyInput\n  const promptHash = "
            },
            {
              "line": 470,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "  mode = '',\n  } = params;\n\n  const requestId = `call-obj-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n\n  // Generate stable task key: model + actionName + hash(prompt/messages) + hash(attachments) + mode\n  // Avoid concatenating long prompt/messages into keyInput\n  const promptHash = "
            }
          ]
        },
        {
          "file": "lib/ai/protocol.ts",
          "issues": [
            {
              "line": 34,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "=== -1) {\n    return null;\n  }\n  \n  const start = openIndex + openTag.length;\n  const content = text.substring(start, closeIndex).trim();\n  \n  return content;\n}\n\n\n"
            }
          ]
        },
        {
          "file": "lib/openai-client.ts",
          "issues": [
            {
              "line": 53,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "t, options?: RequestInit) => {\n    const requestId = `req-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n    const startTime = Date.now();\n    let attemptCount = 0;\n    \n    // CRITICAL: Respect options.signal from llm.ts (do not override)\n    // If options.signal exists, use it; otherwi"
            }
          ]
        },
        {
          "file": "lib/preview-ui-prd-bundle.generated.ts",
          "issues": [
            {
              "line": 399,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "PostfixModifierPosition;\n      let classGroupId = getClassGroupId(hasPostfixModifier ? baseClassName.substring(0, maybePostfixModifierPosition) : baseClassName);\n      if (!classGroupId) {\n        if (!hasPostfixModifier) {\n          result = originalClassName + (result.length > 0 ? \" \" + result : r"
            }
          ]
        },
        {
          "file": "lib/safe/preview.ts",
          "issues": [
            {
              "line": 19,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "eturn 'undefined';\n  \n  if (typeof x === 'string') {\n    if (x.length <= max) return x;\n    return x.substring(0, max) + '...';\n  }\n  \n  if (typeof x === 'number' || typeof x === 'boolean') {\n    return String(x);\n  }\n  \n  // 对象类型：尝试 JSON.stringify\n  if (typeof x === 'object') {\n    try {\n      cons"
            },
            {
              "line": 31,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "{\n      const json = JSON.stringify(x);\n      if (json.length <= max) return json;\n      return json.substring(0, max) + '...';\n    } catch {\n      return '[Object]';\n    }\n  }\n  \n  return String(x);\n}\n\n/**\n * 确保值为字符串类型，否则抛出错误\n * 用于需要强保证的地方（如业务逻辑中的 substring 调用）\n * @param x 输入值\n * @param name 变量名（用于"
            }
          ]
        },
        {
          "file": "lib/ui/prompt-budget.ts",
          "issues": [
            {
              "line": 22,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": "\n  if (input.length <= maxChars) {\n    return input;\n  }\n  \n  // 尝试在单词边界截断\n  const truncated = input.substring(0, maxChars);\n  const lastSpace = truncated.lastIndexOf(' ');\n  const lastNewline = truncated.lastIndexOf('\\n');\n  const cutPoint = Math.max(lastSpace, lastNewline, maxChars - 100);\n  \n  re"
            },
            {
              "line": 27,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "Of('\\n');\n  const cutPoint = Math.max(lastSpace, lastNewline, maxChars - 100);\n  \n  return truncated.substring(0, cutPoint) + '\\n...<clamped>';\n}\n\n/**\n * 安全转换为字符串，非string类型返回fallback\n * @param x 输入值\n * @param fallback 默认值（默认空字符串）\n * @returns 字符串\n */\nexport function safeString(x: unknown, fallback: s"
            },
            {
              "line": 67,
              "match": ".substring(",
              "hasTypeCheck": true,
              "context": " Error(\n    `[PromptBudget] ${name} must be string, got ${type}. ` +\n    `Value: ${JSON.stringify(x).substring(0, 100)}`\n  );\n}\n\n/**\n * 估算token数（粗略：1 token ≈ 4 字符）\n */\nexport function estimateTokens(text: string): number {\n  return Math.ceil((text || '').length / 4);\n}\n\n/**\n * Prompt 片段（用于预算控制）\n */\n"
            }
          ]
        },
        {
          "file": "lib/ui/ui-compiler.ts",
          "issues": [
            {
              "line": 208,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "i.selectorHint} -> ${i.action}(${i.payload})`).join('\\n')}\n\nBase HTML (first 2000 chars):\n${baseHtml.substring(0, 2000)}...\n\n# Output Format\nOutput ONLY one tagged block:\n<AI_PATCH>\n[\n  {\"op\": \"SET_ATTR\", \"selector\": \"button.submit\", \"name\": \"data-action\", \"value\": \"submit\"},\n  {\"op\": \"SET_ATTR\", \"s"
            },
            {
              "line": 49,
              "match": ".substr(",
              "hasTypeCheck": false,
              "context": "Label, aiConfig } = options;\n  const requestId = `compile-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n\n  try {\n    const textModel = getTextModel(aiConfig);\n    const openaiClient = getOpenAIClient(aiConfig);\n\n    // ==================== Track 1: Build UISpec (AI JSON, small budget) ="
            }
          ]
        },
        {
          "file": "lib/ui-generation-helpers.ts",
          "issues": [
            {
              "line": 158,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "ncated: true,\n  });\n  \n  // Try to truncate at a safe boundary (end of tag)\n  const truncated = html.substring(0, maxLength);\n  const lastTagEnd = truncated.lastIndexOf('>');\n  \n  if (lastTagEnd > maxLength * 0.9) {\n    // Safe to truncate at tag boundary\n    return truncated.substring(0, lastTagEnd"
            },
            {
              "line": 163,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " \n  if (lastTagEnd > maxLength * 0.9) {\n    // Safe to truncate at tag boundary\n    return truncated.substring(0, lastTagEnd + 1) + '\\n<!-- Truncated due to size limit -->';\n  }\n  \n  // Fallback: hard truncate\n  return truncated + '\\n<!-- Truncated due to size limit -->';\n}\n\n/**\n * Enforces hard cap"
            }
          ]
        },
        {
          "file": "utils/codeToPrdTable.ts",
          "issues": [
            {
              "line": 23,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "-z]+/);\n    if (match) {\n      return match[0].split('').filter(c => c === c.toUpperCase()).join('').substring(0, 3).toUpperCase();\n    }\n  }\n\n  // 从文件名提取\n  const nameWithoutExt = fileName.replace(/\\.(tsx|ts|jsx|js)$/, '');\n  const words = nameWithoutExt.split(/[-_]/).filter(w => w.length > 0);\n  \n "
            },
            {
              "line": 33,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "  \n  if (words.length > 0) {\n    // 取每个单词的首字母\n    return words.map(w => w[0].toUpperCase()).join('').substring(0, 3);\n  }\n\n  // 默认：取前3个大写字母\n  const upperCase = nameWithoutExt.split('').filter(c => c === c.toUpperCase()).join('');\n  return upperCase.substring(0, 3) || 'GEN';\n}\n\n/**\n * 识别UI区域\n */\nfunc"
            },
            {
              "line": 38,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "upperCase = nameWithoutExt.split('').filter(c => c === c.toUpperCase()).join('');\n  return upperCase.substring(0, 3) || 'GEN';\n}\n\n/**\n * 识别UI区域\n */\nfunction identifyZone(element: string, context: string): string {\n  const lowerElement = element.toLowerCase();\n  const lowerContext = context.toLowerCa"
            }
          ]
        },
        {
          "file": "utils/demo-router.ts",
          "issues": [
            {
              "line": 305,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "enEnd = html.indexOf('>', bodyStart);\n        if (bodyOpenEnd !== -1) {\n          bodyContent = html.substring(bodyOpenEnd + 1);\n        }\n      }\n    }\n\n    // Update body content\n    if (iframeDoc.body) {\n      iframeDoc.body.innerHTML = bodyContent;\n    } else {\n      // If body doesn't exist, cr"
            }
          ]
        },
        {
          "file": "utils/html-parser.ts",
          "issues": [
            {
              "line": 563,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "检查闭合标签，减少深度\n    const closingTagRegex = new RegExp(`</${tag}>`, 'gi');\n    const nextMatch = cleaned.substring(match.index + match[0].length);\n    if (closingTagRegex.test(nextMatch)) {\n      // 找到闭合标签后，深度会自然减少\n    }\n  }\n\n  return structure.join('\\n'); // 移除数量限制，返回所有提取的结构\n}\n\n/**\n * 提取HTML中的所有可见文字内容\n"
            },
            {
              "line": 683,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " &quot;\n    .replace(/&#39;/g, \"'\") // 替换 &#39;\n    .replace(/\\s+/g, ' ') // 合并多个空格\n    .trim()\n    .substring(0, 10000); // 限制长度，避免过长\n  \n  return {\n    buttons: buttons.filter(Boolean),\n    labels: labels.filter(Boolean),\n    menuItems: menuItems.filter(Boolean),\n    cardTitles: cardTitles.filter(B"
            },
            {
              "line": 891,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "保使用相同的文字）：**\n${textContent.cardContent.slice(0, 5).map((content, idx) => `- 卡片${idx + 1}: \"${content.substring(0, 200)}${content.length > 200 ? '...' : ''}\"`).join('\\n')}`);\n    }\n    \n    if (textSections.length > 0) {\n      sections.push(`**=== HTML文字内容（必须严格遵守） ===**\n\n${textSections.join('\\n\\n')}\n"
            }
          ]
        },
        {
          "file": "utils/prdGenerator.ts",
          "issues": [
            {
              "line": 1229,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "            // Remove the entire type definition\n                      componentCode = componentCode.substring(0, start) + componentCode.substring(end);\n                      foundEnd = true;\n                      break;\n                    }\n                  }\n                  i++;\n              "
            },
            {
              "line": 1229,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "e definition\n                      componentCode = componentCode.substring(0, start) + componentCode.substring(end);\n                      foundEnd = true;\n                      break;\n                    }\n                  }\n                  i++;\n                }\n                \n               "
            },
            {
              "line": 1241,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": " if (lineEnd === -1) lineEnd = componentCode.length;\n                  componentCode = componentCode.substring(0, start) + componentCode.substring(lineEnd);\n                }\n                \n                // Reset regex lastIndex to avoid infinite loop\n                typeRegex.lastIndex = start;"
            },
            {
              "line": 1241,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "nentCode.length;\n                  componentCode = componentCode.substring(0, start) + componentCode.substring(lineEnd);\n                }\n                \n                // Reset regex lastIndex to avoid infinite loop\n                typeRegex.lastIndex = start;\n              }\n              \n    "
            }
          ]
        },
        {
          "file": "utils/ui-summary-extractor.ts",
          "issues": [
            {
              "line": 110,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "i.textContent?.trim() || '';\n      if (text) {\n        summary.listItems.push({\n          text: text.substring(0, 100), // Limit length\n          selector: `li:nth-of-type(${index + 1})`,\n        });\n      }\n    });\n\n    // Extract cards (divs with card-like classes or structure)\n    const cards = b"
            },
            {
              "line": 124,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": "ent?.trim() || '';\n      if (title || content) {\n        summary.cards.push({\n          title: title.substring(0, 50),\n          content: content.substring(0, 200),\n          selector: card.id ? `#${card.id}` : `.card:nth-of-type(${index + 1})`,\n        });\n      }\n    });\n  } catch (error) {\n    co"
            },
            {
              "line": 125,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": ") {\n        summary.cards.push({\n          title: title.substring(0, 50),\n          content: content.substring(0, 200),\n          selector: card.id ? `#${card.id}` : `.card:nth-of-type(${index + 1})`,\n        });\n      }\n    });\n  } catch (error) {\n    console.error('[extractUISummaryFromHTML] Error"
            },
            {
              "line": 197,
              "match": ".substring(",
              "hasTypeCheck": false,
              "context": ";\n      const text = match[1]?.trim();\n      if (text) {\n        summary.listItems.push({ text: text.substring(0, 100) });\n        liCount++;\n      }\n    }\n  } catch (error) {\n    console.error('[extractUISummaryFromReact] Error:', error);\n  }\n\n  return summary;\n}\n\n/**\n * Format UI summary as contex"
            }
          ]
        }
      ]
    },
    "htmlRenderIssues": {
      "count": 11,
      "entries": [
        {
          "file": "app/actions/generate-tailwind-config.ts",
          "issues": [
            {
              "line": 163,
              "match": "eval(",
              "hasSanitize": false,
              "context": "cript 对象\n      try {\n        // 尝试解析（包装成对象以便验证）\n        const testCode = `(${configCode})`;\n        eval(testCode);\n      } catch (parseError) {\n        logError('⚠️ [generateTailwindConfig] 生成的配置无法解析，使用默认配置', parseError);\n        // 如果解析失败，返回默认配置\n        configCode = `{\n  theme: {\n    extend: {\n   "
            }
          ]
        },
        {
          "file": "components/canvas/BabelExecutor.tsx",
          "issues": [
            {
              "line": 35,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "rn;\n    }\n\n    // Clear previous content\n    if (containerRef.current) {\n      containerRef.current.innerHTML = '';\n    }\n    setError(null);\n    setIsCompiling(true);\n\n    const execute = async () => {\n      try {\n      // Step 1: Inject Tailwind config if provided\n      if (configString) {\n       "
            },
            {
              "line": 156,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "   // Display error in container\n        if (containerRef.current) {\n          containerRef.current.innerHTML = `\n            <div style=\"padding: 20px; color: #ef4444; background: #fee2e2; border-radius: 8px;\">\n              <h3 style=\"margin: 0 0 10px 0;\">编译错误</h3>\n              <pre style=\"margin"
            },
            {
              "line": 50,
              "match": "new Function",
              "hasSanitize": false,
              "context": "       const existingConfig = (window as any).tailwind?.config || {};\n            const newConfig = new Function('return ' + configCode)();\n            (window as any).tailwind.config = {\n              ...existingConfig,\n              ...newConfig,\n            };\n            log('🎨 [BabelExecutor] "
            },
            {
              "line": 127,
              "match": "new Function",
              "hasSanitize": false,
              "context": "anspiled code\n      // Create a function with React dependencies in scope\n      const executeCode = new Function(\n        'React',\n        'createRoot',\n        'useState',\n        'useEffect',\n        'renderTarget',\n        transpiledCode || ''\n      );\n\n      // Execute with React dependencies\n  "
            }
          ]
        },
        {
          "file": "components/canvas/HtmlSandbox.tsx",
          "issues": [
            {
              "line": 101,
              "match": "new Function",
              "hasSanitize": false,
              "context": "CT') {\n      try {\n        // Execute injected script using Function constructor\n        const fn = new Function(message.script);\n        fn();\n      } catch (error) {\n        console.error('[Sandbox] Error executing injected script:', error);\n        if (window.parent) {\n          window.parent.pos"
            }
          ]
        },
        {
          "file": "components/canvas/HtmlSandboxRenderer.tsx",
          "issues": [
            {
              "line": 471,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "px-4 py-3';\n          th.textContent = col;\n          tr.appendChild(th);\n        });\n        thead.innerHTML = '';\n        thead.appendChild(tr);\n        thead.className = 'border-b border-slate-200 bg-slate-50 text-slate-700';\n      }\n      let tbody = table.querySelector('tbody');\n      if (!tbod"
            },
            {
              "line": 480,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "{\n        tbody = doc.createElement('tbody');\n        table.appendChild(tbody);\n      }\n      tbody.innerHTML = '';\n      rows.forEach((cells) => {\n        const tr = doc.createElement('tr');\n        tr.className = 'data-item';\n        tr.setAttribute('data-filter', 'active');\n        cells.forEach("
            },
            {
              "line": 596,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "ector: string, html: string): void => {\n        injector.modifyDOM(selector, (el) => {\n          el.innerHTML = html;\n        });\n      },\n\n      /**\n       * Add class to elements\n       */\n      addClass: (selector: string, className: string): void => {\n        injector.modifyDOM(selector, (el) =>"
            }
          ]
        },
        {
          "file": "components/canvas/LivePreview.tsx",
          "issues": [
            {
              "line": 368,
              "match": "new Function",
              "hasSanitize": false,
              "context": "s*=\\s*\\(/.test(cleaned);\n      \n      let wrappedCode: string;\n      // 注意：图标声明不在 wrappedCode 中，而是在 new Function() 的函数体开头添加\n      // 这样可以确保图标变量在编译后的代码执行前就已经初始化\n      const allDeclarations = declarations.join('\\n');\n      \n      if (isFunctionDeclaration || isConstFunction || isArrowFunction) {\n     "
            },
            {
              "line": 432,
              "match": "new Function",
              "hasSanitize": false,
              "context": "actory: Function;\n      try {\n        // 使用 IIFE 包装代码，确保图标变量在代码执行前就已经初始化\n        ComponentFactory = new Function(\n          'React',\n          'LucideIcons',\n          'Recharts',\n          'PreviewUI',\n          `\n          // 确保依赖项可用\n          if (typeof React === 'undefined' || !React) {\n        "
            }
          ]
        },
        {
          "file": "components/canvas/MermaidDiagram.tsx",
          "issues": [
            {
              "line": 143,
              "match": "dangerouslySetInnerHTML",
              "hasSanitize": false,
              "context": "Height: 'auto' } : { minHeight: 'auto', maxHeight: 'none' }}\n    >\n      <div ref={svgContainerRef} dangerouslySetInnerHTML={{ __html: svg }} style={compact ? { width: '100%', height: 'auto', display: 'inline-block' } : undefined} />\n    </div>\n  );\n}\n"
            }
          ]
        },
        {
          "file": "components/canvas/PreviewFrame.tsx",
          "issues": [
            {
              "line": 47,
              "match": "dangerouslySetInnerHTML",
              "hasSanitize": false,
              "context": ": '100%',\n          minHeight,\n          boxSizing: 'border-box',\n        }}\n      >\n        <style dangerouslySetInnerHTML={{ __html: PREVIEW_FRAME_CSS }} />\n        <div\n          ref={ref ?? undefined}\n          className=\"preview-frame__inner w-full h-full\"\n          style={{ minHeight, boxSizin"
            }
          ]
        },
        {
          "file": "components/canvas/ReactJitRenderer.tsx",
          "issues": [
            {
              "line": 120,
              "match": "dangerouslySetInnerHTML",
              "hasSanitize": false,
              "context": " className=\"text-red-500 text-sm mb-2\">交互逻辑加载失败，已降级为静态视图</div>\n        {rawHtml && (\n          <div dangerouslySetInnerHTML={{ __html: rawHtml }} />\n        )}\n        {!rawHtml && (\n          <div className=\"text-red-400 text-xs\">\n            {error.message}\n          </div>\n        )}\n      </div>"
            },
            {
              "line": 43,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "rn;\n    }\n\n    // Clear previous content\n    if (containerRef.current) {\n      containerRef.current.innerHTML = '';\n    }\n    setError(null);\n\n    try {\n      // 1. 预处理：如果用户给的是纯 HTML，先把它包装成 React 组件代码\n      let codeToRun = rawCode;\n      if (!codeToRun && rawHtml) {\n        codeToRun = convertHtmlTo"
            },
            {
              "line": 69,
              "match": "new Function",
              "hasSanitize": false,
              "context": "e;\n\n      // 3. 构造组件：将编译后的代码转换为可执行函数\n      // 注入 React, useState 等依赖，使其在函数作用域内可用\n      const func = new Function(\n        'React',\n        'useState',\n        'useEffect',\n        'Recharts',\n        'createRoot',\n        'renderTarget',\n        `\n          // Make React available in scope\n         "
            }
          ]
        },
        {
          "file": "utils/demo-router.ts",
          "issues": [
            {
              "line": 312,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "\n        }\n      }\n    }\n\n    // Update body content\n    if (iframeDoc.body) {\n      iframeDoc.body.innerHTML = bodyContent;\n    } else {\n      // If body doesn't exist, create it\n      const body = iframeDoc.createElement('body');\n      body.innerHTML = bodyContent;\n      iframeDoc.documentElement."
            },
            {
              "line": 316,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": " // If body doesn't exist, create it\n      const body = iframeDoc.createElement('body');\n      body.innerHTML = bodyContent;\n      iframeDoc.documentElement.appendChild(body);\n    }\n\n    // Reattach navigation handler (new content may have new nav elements)\n    // Use requestAnimationFrame for bette"
            }
          ]
        },
        {
          "file": "utils/html-to-react-adapter.ts",
          "issues": [
            {
              "line": 330,
              "match": "dangerouslySetInnerHTML",
              "hasSanitize": false,
              "context": "y-80\">`;\n    }\n  );\n\n  // 7. 组装组件 (Assembly)\n  // 使用亮色背景容器\n  const styleTag = styles \n    ? `<style dangerouslySetInnerHTML={{ __html: ${JSON.stringify(styles)} }} />`\n    : '';\n\n  const componentCode = `\n    ${tailwindConfigScript ? `(() => { ${tailwindConfigScript} })();` : ''}\n\n    const App_Ligh"
            }
          ]
        },
        {
          "file": "utils/prdGenerator.ts",
          "issues": [
            {
              "line": 1121,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "l) {\n              if (el.textContent && el.textContent.indexOf('加载中') !== -1) {\n                el.innerHTML = '<div style=\"padding:20px;color:#64748b;text-align:center;font-size:13px;\">请使用「导出 PRD」重新生成此文件以正确渲染 UI</div>';\n              }\n            });\n            return;\n          }\n          cons"
            },
            {
              "line": 1165,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "          console.warn('⚠️ Node ' + nodeData.title + ' has empty UI code');\n              container.innerHTML = '<div style=\"padding: 20px; color: #9ca3af; text-align: center;\">暂无 UI 代码</div>';\n              return;\n            }\n\n            try {\n              if (nodeData.isHtml) {\n              "
            },
            {
              "line": 1181,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": ") + '<div class=\"prd-ui-viewport\">' + after + '</div>';\n                }\n                container.innerHTML = '';\n                container.className = 'device-sandbox';\n                var innerDiv = document.createElement('div');\n                innerDiv.className = 'prd-ui-sandbox-inner';\n     "
            },
            {
              "line": 1186,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "cssText = 'width:100%;height:100%;min-height:100%;box-sizing:border-box;';\n                innerDiv.innerHTML = htmlContent;\n                container.appendChild(innerDiv);\n                return;\n              }\n\n              console.log('🔄 [mountComponents] Transforming code for:', nodeData.tit"
            },
            {
              "line": 1347,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "d successfully');\n              \n              // Clear container and mount\n              container.innerHTML = '';\n              console.log('🎯 [mountComponents] Mounting component to container:', containerId);\n              \n              const root = window.ReactDOM.createRoot(container);\n      "
            },
            {
              "line": 1356,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "le.error('❌ Failed to mount component for ' + nodeData.title + ':', error);\n              container.innerHTML = '<div style=\"padding: 20px; color: #ef4444; text-align: center;\">' +\n                '<p>⚠️ UI 组件加载失败</p>' +\n                '<p style=\"font-size: 12px; margin-top: 8px;\">' + (error.messag"
            },
            {
              "line": 1414,
              "match": "innerHTML =",
              "hasSanitize": false,
              "context": "l) {\n              if (el.textContent && el.textContent.indexOf('加载中') !== -1) {\n                el.innerHTML = '<div style=\"padding: 20px; color: #94a3b8; text-align: center; font-size: 13px;\"><p>UI 未能渲染</p><p style=\"margin-top: 8px;\">请用浏览器打开此 HTML 并允许加载脚本（React/Babel CDN）</p></div>';\n             "
            }
          ]
        }
      ]
    }
  },
  "issues": [],
  "summary": {
    "buildStatus": "pass",
    "lintStatus": "pass",
    "typecheckStatus": "pass",
    "llmEntryCount": 11,
    "jsonIssueCount": 28,
    "networkIssueCount": 10,
    "substringIssueCount": 23,
    "htmlRenderIssueCount": 11
  }
}
```
