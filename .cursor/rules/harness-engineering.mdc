---
description: Harness Engineering 方法论——文档即目录、架构约束、可观测性与工程师角色（基于 OpenAI 工程实践）
globs: ["**/*.md", "**/AGENTS.md", "**/ARCHITECTURE.md", "**/docs/**"]
alwaysApply: true
priority: 90
---

# Harness Engineering

基于 [OpenAI Harness Engineering](https://openai.com/zh-Hans-CN/index/harness-engineering/)：将脚手架、反馈循环、文档与架构约束编码为机器可读产物，由智能体在代码生成、测试与可观测性流程中执行任务。

## 1. 文档即事实来源

### AGENTS.md 是目录表，不是百科全书
- **AGENTS.md** 控制在约 100 行内，作为「入口索引」。
- 只列：项目目标、关键文档链接、架构边界、与智能体的约定。
- 详细内容放在 `docs/`，通过链接引用。

### docs/ 结构化知识库
- **设计文档**：产品规范、PRD、交互说明。
- **架构说明**：ARCHITECTURE.md、依赖层级、模块边界。
- **执行计划**：里程碑、任务分解、验收标准。
- **参考资料**：外部规范、设计系统、API 文档。
- 通过 CI 或脚本（可选）校验：文档新鲜度、交叉链接、目录结构。

## 2. 架构约束

### 依赖层级（可依项目调整）
建议流向：**Types → Config → Repo → Service → Runtime → UI**
- Types：共享类型与 schema（如 zod）。
- Config：环境与配置。
- Repo：数据访问与仓储。
- Service：业务逻辑。
- Runtime：运行时与可观测性。
- UI：界面与交互。

### 强制执行方式
- 用 **机械规则**（如 ESLint 插件、import 边界规则）禁止跨层逆向依赖。
- 用 **结构测试**（如依赖图校验、层合规检查）在 CI 中验证。
- 智能体在编写代码时遵守上述层级，不破坏分层。

## 3. 可观测性（可选接入）

- 将日志、指标、追踪接入智能体可读接口（如 LogQL、PromQL 或约定格式）。
- 在**隔离环境**中复现缺陷，由智能体提出修复并自动验证。
- 前端可配合 Chrome DevTools 协议或构建时/运行时检查。

## 4. 工程师角色

- **委托**：可行性分析、样板代码、初始实现、重复性任务 → 交给智能体。
- **审查**：设计选择、性能、安全、可维护性 → 由人做评审与决策。
- **负责**：战略方向、架构设计、新颖问题与最终交付 → 由人拥有。

## 5. 工作流

1. **描述任务**：用自然语言或结构化提示说明目标与约束。
2. **执行智能体**：智能体使用标准开发工具（编辑器、CLI、脚本、技能）产出代码/配置/文档。
3. **审查与反馈**：人审 PR/变更，给出结构化反馈。
4. **迭代**：智能体根据反馈修改，直至满足标准。

## 6. 与本项目规则的关系

- 写代码时仍遵守 **ARCHITECTURE.md**、**commonsense.mdc**、**efficiency.mdc**。
- 本规则侧重「文档结构 + 架构边界 + 人机分工」，与现有技术栈规则互补。
