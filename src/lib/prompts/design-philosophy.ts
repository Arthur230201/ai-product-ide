/**
 * 设计理念（来自世界级/500 强级产品 UI 调研归纳）
 * 通用表述，不依赖任何具体行业或产品名；用于生成前注入，约束高质量 UI 的共性。
 * @see docs/DESIGN_PHILOSOPHY_FROM_LEADING_PRODUCTS.md
 */

/**
 * 高质量 UI 的共性理念（生成前注入，与 HIGH_QUALITY_UI_DEFINITION、UI_CONSTITUTION 配合）
 */
export const DESIGN_PHILOSOPHY_PRINCIPLES = `
# 高质量 UI 的共性理念（必须体现）

- **内容优先**：界面服务内容与任务；留白、字体、颜色服务于可读与焦点。每屏一个明确焦点（主标题或主 CTA），其余为支撑层级，不抢戏。
- **清晰与层级**：标题与正文有明确字号、字重层级；通过间距与对比度区分主次，用户能快速理解「这是什么、主要做什么」。
- **一致性**：同类型元素用同一组件与样式；颜色、圆角、阴影统一，不在一页内混用多种风格；导航与操作位置可预期。
- **留白与节奏**：内容区与区块间距遵循稳定网格（如 p-4/p-6、gap-4/gap-6）；留白用于分隔与突出重点，避免拥挤。
- **克制用色**：单一主色用于主操作/CTA；背景以白或浅灰为主；状态与强调色控制对比度，不大面积高饱和堆砌。
- **主内容区有实质**：主区与页面目标一致（数据则列表/表格+数据，表单则完整表单项，详情则完整信息块，仪表盘则统计与入口）；禁止大面积空白或仅占位文案。
- **组件化**：使用统一组件（Button、Card、Input、PageHeader、ListItem 等）保证一致与可访问；交互有明确状态（hover、focus）。
- **数据场景**：若为数据/仪表类，先总览后细节；核心指标突出，减少无关装饰；色彩与图例一致。
`.trim();
